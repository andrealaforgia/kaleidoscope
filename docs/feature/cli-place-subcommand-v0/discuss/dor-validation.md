# Definition of Ready — Validation

Feature: `cli-place-subcommand-v0`
Reviewer: Luna (self-review at DISCUSS close; second pass scheduled
at handoff to DESIGN per `nw-po-review-dimensions` skill).
Date: 2026-05-19.

## Per-story DoR (9-item hard gate)

### US-01: Operator manually bootstraps a Cinder item without writing Rust or running ingest

| # | DoR Item | Status | Evidence |
|---|----------|--------|----------|
| 1 | Problem statement clear, domain language | PASS | "What she canNOT do today is `place` a Cinder item DIRECTLY — without running `ingest` first. Three operationally distinct gaps fall out of this: 1. Bootstrap items that exist outside the Lumen ingest flow ... 2. Set up a controlled test scenario ... 3. Recover the Cinder catalog from a manifest after a snapshot corruption ... All three reduce to 'give me one CLI invocation that calls `TieringStore::place(tenant, &ItemId, tier, SystemTime::now())` and tells me, on stdout, that the placement happened.'" Uses tier, place, ItemId, TieringStore, Cinder, batch, bootstrap, manifest, snapshot, hot/warm/cold, overwrite-semantics, `get_entry`, lower-case, `--observe-otlp`, `cinder.place.count` — all domain terms grounded in the existing Cinder/CLI implementation and the established self-observe wire format. |
| 2 | User/persona identified | PASS | Priya the platform operator, multi-tenant Kaleidoscope deployment for a fintech, already uses `kaleidoscope-cli ingest`, `kaleidoscope-cli stats`, `kaleidoscope-cli migrate`, `kaleidoscope-cli list-items` daily (inherited from the eight predecessor features). Uses standard Unix text tools (`grep`, `cut`, `awk`) on stdout output. Runs the subcommand inside shell loops for bulk bootstrap. Expects fail-fast behaviour (non-zero exit + descriptive stderr) on invalid tier names. Does NOT expect the subcommand to verify the item doesn't already exist (overwrite-semantics is the documented underlying API behaviour). Does NOT expect the subcommand to touch the Lumen store. |
| 3 | 3+ domain examples with real data | PASS | Five examples: happy-path bootstrap with `acme/bootstrap-00001` in Hot and the literal one-line stdout shown; edge case (overwrite-semantics) with `acme/bootstrap-00007` Hot→Cold showing the underlying API IS overwrite-semantics per `crates/cinder/src/store.rs:78-81, :140-152` and the CLI faithfully reports the NEW tier; error case (invalid tier) with `HOT` showing the empty-stdout + non-empty-stderr + non-zero-exit + no-store-mutation contract; observability case (`--observe-otlp`) with a three-iteration shell loop showing three `cinder.place.count` lines appended to the sidecar; tenant-isolation case with `acme` and `globex` both holding an item id `acme/bootstrap-00001` showing only `acme`'s item is overwritten. Real values throughout: `acme`, `globex`, `acme/bootstrap-00001`, `acme/bootstrap-00007`, `acme/bootstrap-0001`, `acme/bootstrap-0002`, `acme/bootstrap-0003`, `/tmp/k-data`, `/tmp/kdata`, `/tmp/data`, `/tmp/cinder.otlp.json`, real tier names (`hot`, `warm`, `cold`), real source-file references with line numbers. |
| 4 | UAT scenarios (3-7 in Given/When/Then) | PASS | 5 scenarios: happy-path bootstrap in Hot (OK1), overwrite-semantics over an existing item (OK2), invalid-tier fail-fast (OK3), `--observe-otlp` emission (OK4), tenant-isolation (`place(acme, ...)` does not mutate `globex`'s same-named item). |
| 5 | AC derived from UAT | PASS | 10 AC bullets, each maps to an observable byte-level / state-level invariant from a scenario (one literal stdout line on happy-path success; overwrite stdout line shows the NEW tier; post-call `get_entry().tier == tier` for happy-path; post-call `get_entry().tier == new_tier` for overwrite; non-zero exit + stderr containing invalid tier value for invalid-tier; tenant-isolation `get_entry(globex, ...).tier` unchanged; exactly one `cinder.place.count` line per call when `--observe-otlp` is set, no file when it is not; Lumen store byte-equivalent before/after every invocation; new acceptance test file added; locked test files continue to pass green unmodified; `print_usage` update optional; no new external dependency). |
| 6 | Right-sized | PASS | 1 story, 1 function-level addition in `lib.rs` (new `place(...)` library function — reuses existing `parse_tier`, `tier_lowercase`, `cinder_base`, `CinderRecorder`, `CinderToOtlpJsonWriter`, `Error::InvalidTier`, `Error::CinderOpen`, `Error::Io`), 1 dispatch wiring change in `main.rs` (new `Some("place")` arm + new `run_place(...)` / `run_place_with<O: Write>(...)` helpers + `print_usage` update), 1 new test file with 5 scenarios, 1 manifest entry. Estimated effort: well under 1 day. Comparable to the immediate predecessors (`cli-migrate-subcommand-v0`, `cli-migrate-observe-otlp-v0`); the structural surface area is the same. |
| 7 | Technical notes identify constraints | PASS | File paths, line numbers, manifest entry, dependency posture (no new external dependency — `cinder::TieringStore::place`, `get_entry`, `FileBackedTieringStore::open`, `cinder::CinderRecorder`, `self_observe::CinderToOtlpJsonWriter`, `Tier` enum, `ItemId::new`, `cinder_base()` helper, `parse_tier()` helper, `tier_lowercase()` helper, `parse_positional` helper, `parse_observe_otlp` helper, `Error::InvalidTier` variant, `aegis::TenantId`, `std::time::SystemTime::now()` all already wired). The explicit non-prescription of internal implementation details beyond the choice of one new free function shape (DESIGN-locked per `wave-decisions.md` D-FunctionShape) and the choice of whether to promote `parse_tier` to `pub(crate)`. The hard rule "DO NOT modify any locked test file" is restated as D-LockedTests. The hard rule "DO NOT design implementation" is honoured: the AC and KPIs pin wire-observable invariants only; the exact function signature, the exact stderr wording (inherited from `Error::InvalidTier` Display), and the recorder-factor question are explicitly DESIGN's calls. |
| 8 | Dependencies tracked | PASS | All Cinder-side APIs exist (`crates/cinder/src/store.rs:78-81, :89, :119, :140-152, :151`); `cinder::FileBackedTieringStore` is already used by `ingest()` (`crates/kaleidoscope-cli/src/lib.rs:185-188`), `migrate()` (lines 445-446), `list_items()` (lines 489-490), `stats_with_tiers()` (lines 377-378); `cinder::CinderRecorder` already used by `migrate()`'s no-flag arm (line 443); `self_observe::CinderToOtlpJsonWriter` already imported (line 65) and used by `ingest()` (lines 172-174) and `migrate()` (line 441); `cinder::Tier` and `cinder::ItemId` already imported (line 58); `cinder_base(data_dir)` helper at lines 130-132; `parse_tier` helper at lines 505-512; `tier_lowercase` helper at lines 519-525; `Error::InvalidTier { value: String }` variant at lines 79-81; `parse_positional` helper at `crates/kaleidoscope-cli/src/main.rs:328-334`; `parse_observe_otlp` helper at lines 178-192; `aegis::TenantId` already a dependency. No unresolved internal or external dependencies. |
| 9 | Outcome KPIs defined with measurable targets | PASS | Maps to OK1-CLI-place-success (principal: 100% of valid invocations produce the exact stdout report line AND post-call `get_entry().tier == tier`), OK2-CLI-place-overwrite-semantics (100% of re-place invocations succeed faithfully — no CLI special case; the underlying API is overwrite-semantics per `crates/cinder/src/store.rs:78-81, :140-152`), OK3-CLI-place-invalid-tier-fail-fast (100% of non-lower-case-hot/warm/cold invocations produce non-zero exit + stderr containing the invalid value + no store mutation), OK4-CLI-place-observe-otlp-emission (100% of `--observe-otlp` invocations produce exactly one `cinder.place.count` OTLP-JSON line per call; 100% of no-flag invocations produce no on-disk OTLP file artefacts). All four targets are quantitative and falsifiable via the named test file. |

### US-01 DoR Status: PASSED

## Feature-level DoR

| # | Item | Status | Evidence |
|---|------|--------|----------|
| F1 | Journey artefact (visual) | NOT PRODUCED — accepted | Per `wave-decisions.md`, the journey is so thin (one operator action: invoke `place <tenant> <data_dir> <item_id> <tier> [--observe-otlp <path>]` and read stdout) that a separate journey visual would duplicate the Elevator Pitch in US-01. The eight reference features in this cluster collectively cover the underlying CLI operator mental model. Acknowledged gap; will not be remediated. |
| F2 | Journey artefact (YAML schema) | NOT PRODUCED — accepted | Same rationale as F1. The journey schema in `nw-design-methodology` is for multi-step user journeys; this feature has one step. |
| F3 | Journey artefact (Gherkin `.feature`) | INTENTIONALLY OMITTED | Per the project convention (inherited from the eight reference features and noted in the task brief): this project's acceptance idiom is Rust `#[test]` functions with `// Given / // When / // Then` comment blocks, not Gherkin `.feature` files. The Given/When/Then text lives inside `user-stories.md` (UAT Scenarios sections). DISTILL writes the Rust tests. |
| F4 | Shared artefact registry | NOT PRODUCED — accepted | The cross-feature shared artefacts (`tenant_id` positional argument, `data_dir` positional argument, `lumen_base(data_dir)` and `cinder_base(data_dir)` helpers, `TieringStore::place` / `get_entry` / `migrate` / `list_by_tier` APIs, `CinderRecorder` quiescent + `CinderToOtlpJsonWriter` OTLP wiring patterns, the lower-case tier rendering helper `tier_lowercase` and parsing helper `parse_tier`, the `--observe-otlp <path>` flag convention, the `cinder.place.count` OTLP-JSON wire format) are tracked in the eight reference features and the source code itself. The output-shape contracts (one literal stdout line on success; non-zero exit + stderr line on failure; one `cinder.place.count` line per place call when `--observe-otlp` is set) are restated as System Constraints in `user-stories.md`. |
| F5 | Story map | PASS | `discuss/story-map.md` with one-activity backbone, walking-skeleton justification (N/A — the predecessor cluster's substrate already exists and the Cinder `TieringStore::place` API already exists), single-slice rationale, cross-feature alignment, scope assessment, priority rationale section, all four KPI guardrails articulated. |
| F6 | Prioritization | INTEGRATED into `story-map.md` | The single-slice structure renders a separate `prioritization.md` redundant. The `## Priority Rationale` section in `story-map.md` covers the ordering logic and partial-ship reasoning. |
| F7 | Outcome KPIs | PASS | `discuss/outcome-kpis.md` with OK1 (principal — place-success correctness), OK2 (overwrite-semantics — guardrail against introducing a special-case CLI guard that diverges from the underlying API), OK3 (invalid-tier fail-fast), OK4 (`--observe-otlp` emission), each with quantitative target, baseline, and measurement plan. |
| F8 | User stories | PASS | `discuss/user-stories.md` with US-01, including complete Elevator Pitch with the real entry-point shell command (`kaleidoscope-cli place acme /tmp/data acme/bootstrap-00001 hot`) and the concrete byte-level output (one literal stdout line shown in Domain Example 1, the same literal shape for Examples 2-5 with varying values; the OTLP-JSON sidecar line content shown in Example 4). |
| F9 | Per-slice files | PASS | 1 file under `discuss/slices/` (`slice-01-place-subcommand-bootstraps-item.md`). |
| F10 | Wave decisions | PASS | `discuss/wave-decisions.md` with D1-D4 (pre-wave decisions), D-LowerCase (the explicit task-brief decision), D-Timestamp (no `--placed-at` flag), D-Overwrite (faithful to underlying overwrite-semantics — no CLI special case), D-NoLumenTouch (Cinder-only), D-ObserveOtlp (the only optional flag), D-FunctionShape (DESIGN-locked signature), D-ErrorVariant (no new variant required), D-StderrWording (inherits `Error::InvalidTier` Display), D-NewTestFile (new file location), D-LockedTests (do-not-modify-locked-files hard rule), D-OutOfScope-PlacedAt / D-OutOfScope-Bulk / D-OutOfScope-ExistsCheck / D-OutOfScope-LumenMutation (all four task-brief out-of-scope items), D-NoSSOT. |
| F11 | Right-sized (Elephant Carpaccio) | PASS | 1 story, 1 bounded context (`kaleidoscope-cli` crate), 2 modified files in `src/`, 1 new test file, 1 manifest line-level change. Estimated effort: well under 1 day. `story-map.md` "Scope Assessment: PASS". |
| F12 | SSOT impact assessed | PASS | `wave-decisions.md` D-NoSSOT: no SSOT modification. Same posture as the eight reference features. |
| F13 | Cross-feature contract honoured | PASS | `user-stories.md` System Constraints pin the output-shape contract (one literal stdout line on success; non-zero exit + stderr line on failure; one `cinder.place.count` line per call when `--observe-otlp` is set), the positional-argument convention (extends the existing two-positional shape with two new positional arguments `<item_id> <tier>` — plus the established optional `--observe-otlp <path>` flag), the stream contract (stdout for success, stderr for failure — inherited from the binary's existing main.rs error printer at `crates/kaleidoscope-cli/src/main.rs:73-77`), the lower-case tier convention (mirrors the rendering side at `crates/kaleidoscope-cli/src/lib.rs:519-525` AND the parse side at lines 505-512), the no-Lumen-touch invariant (NEW — `wave-decisions.md` D-NoLumenTouch), the tenant-isolation invariant (inherited from `cinder::TieringStore`), the faithfulness-to-underlying-API contract for the overwrite-semantics case (NEW — D-Overwrite), and the `--observe-otlp` emission shape (inherited from `cli-cinder-otlp-wiring-v0` and `cli-migrate-observe-otlp-v0`). The locked test files for the prior features continue to pass green UNMODIFIED. |

## Anti-pattern scan

| Anti-pattern | Detected? | Notes |
|--------------|-----------|-------|
| Implement-X | No | Story framed as "Priya runs `kaleidoscope-cli place acme /tmp/data acme/bootstrap-00001 hot` and sees the one-line placement report on stdout in milliseconds, exit 0, without writing Rust" — outcome-first from the operator perspective. The three operator decisions enabled (bootstrap items outside the Lumen ingest flow / set up a controlled test scenario / recover from a Cinder snapshot corruption against a manifest) are named explicitly. |
| Generic data | No | Real names: Priya, `acme`, `globex`. Real item ids: `acme/bootstrap-00001`, `acme/bootstrap-00007`, `acme/bootstrap-0001`, `acme/bootstrap-0002`, `acme/bootstrap-0003`, `acme/seed-00001`. Real paths: `/tmp/k-data`, `/tmp/kdata`, `/tmp/data`, `/tmp/cinder.otlp.json`. Real tier names: `hot`, `warm`, `cold` (and the explicit anti-examples `HOT`, `lukewarm`). Real source-file references: `crates/kaleidoscope-cli/src/lib.rs:58, :65, :79-81, :98-100, :130-132, :172-174, :185-188, :251-253, :377-378, :435-444, :441, :443, :445-446, :447, :489-490, :505-512, :519-525`; `crates/cinder/src/store.rs:71-72, :78-81, :89, :119, :140-152, :151`; `crates/kaleidoscope-cli/src/main.rs:54-69, :73-77, :80-157, :178-192, :276-306, :328-334`. |
| Technical AC | No blocking instances | AC pin the wire-observable invariants (one literal stdout line on success with all three fields; empty stdout + non-empty stderr + non-zero exit on the invalid-tier failure path; post-call `get_entry().tier` matches expectation for success; post-call `get_entry().tier == new_tier` for the overwrite case; one `cinder.place.count` OTLP-JSON line per call when `--observe-otlp` is set; no file when it is not; Lumen store byte-equivalence before and after every invocation; tenant-isolation invariant). They reference the structural mirror of the predecessor's recorder construction and `run_migrate` helper shape as implementation patterns but do NOT prescribe novel internal mechanisms; the structural-mirror references are facts about the precedent, not novel design constraints. |
| Technical scenario titles | No | Scenarios titled by user outcome: "Operator bootstraps a fresh item in Hot — sees the placement on stdout", "Operator re-places an existing item with a different tier — overwrite succeeds", "Operator types an invalid tier value — fail-fast with stderr naming the invalid value", "Operator invokes `--observe-otlp` — one `cinder.place.count` line is appended per call", "Tenant isolation — placing `acme/bootstrap-00001` for `acme` does not touch `globex`'s same-named item". Not "TieringStore::place is called with arguments" or "CinderToOtlpJsonWriter::record_place fires once". |
| Oversized story | No | 1 story, 5 UAT scenarios, 10 AC, well under 1 day effort. Comparable to the predecessors. |
| Abstract requirements | No | Every AC has a specific value (string literal `placed tenant=`, `item=`, `tier=`, the `\n` line terminator, substring assertions for stderr content and OTLP-JSON content), a numeric exit code (0 for success, non-zero for failure), or a state-level invariant (pre-call vs post-call `get_entry().tier` equality; Lumen directory byte-equivalence; OTLP-JSON file existence and line count). |

## Reviewer self-pass (Dimension 0 — Elevator Pitch, BLOCKING)

| Story | Section present | Real entry point | Concrete output | Job connection | Verdict |
|-------|-----------------|------------------|-----------------|----------------|---------|
| US-01 | yes (Before / After / Decision enabled triple) | yes — `kaleidoscope-cli place acme /tmp/data acme/bootstrap-00001 hot` (the actual shell command an operator types — NEW subcommand on the existing binary) and the optional `kaleidoscope-cli place acme /tmp/data acme/bootstrap-00001 hot --observe-otlp /tmp/cinder.otlp.json` form | yes — one literal stdout line (`placed tenant=acme item=acme/bootstrap-00001 tier=hot`) shown in the "After" line; companion overwrite-semantics line behaviour described concretely (`tier=cold` after re-place); companion error-case stderr line behaviour described concretely (`HOT` named verbatim); companion observe-otlp file-content shape described concretely (`cinder.place.count` line with `tenant_id=acme` and `tier=hot`) | yes — "Priya can decide and execute three operationally distinct placements from one CLI invocation: 1. 'Bootstrap an item that lives outside Lumen — e.g. an archival batch in cold storage I want Cinder to track — without running a fake `ingest`' ... 2. 'Set up the test scenario: 10 items in Hot, then `evaluate-policy` to observe migrations' ... 3. 'Recover the Cinder catalog from a manifest file after a snapshot corruption' ..." | PASS |

Slice-level check (Dimension 0 item 5): the single slice (Slice
01) is tagged operator-visible (NOT `@infrastructure`). The
story's Elevator Pitch names a real user-invocable CLI command
and the byte-level stdout output the operator's shell session
will display. There is no slice-level infrastructure-only
blocking concern. PASS at slice level.

## Confirmation-bias detection (Dimensions 1-4 self-check)

| Dimension | Check | Verdict |
|-----------|-------|---------|
| 1.1 Technology bias | Story specifies the existing CLI binary, the existing `TieringStore` trait API, and a plain-text one-line stdout shape. No specific JSON parser prescribed; no specific datetime library prescribed (`SystemTime::now()` from `std::time`); the OTLP-JSON sidecar shape is inherited byte-identically from the established `CinderToOtlpJsonWriter` adapter. The output is consumable by standard Unix text tools (`grep`, `cut`, `awk`, `tail -f`) that any operator already has. | PASS |
| 1.2 Happy path bias | Story includes three non-happy-path scenarios out of five: invalid-tier fail-fast (OK3), overwrite-semantics over an existing item (OK2 — technically a "success" but exercises the overwrite branch which is a key faithfulness contract), and tenant-isolation guardrail. The `--observe-otlp` emission scenario (OK4) is also a non-trivial side-effect path that exercises the recorder-construction `match` and the on-disk file-open contract. | PASS |
| 1.3 Availability bias | The chosen output shape (plain-text key=value-on-one-line) is justified by the operator's existing tooling (Unix text utilities) and by the established conventions in the eight predecessor features. The lower-case tier argument constraint (D-LowerCase) is justified by reference to both the rendering-side convention at `crates/kaleidoscope-cli/src/lib.rs:519-525` AND the parse-side convention at lines 505-512 (already used by `migrate` and `list-items`). The `SystemTime::now()`-at-call-site decision (D-Timestamp) is justified against the alternative `--placed-at` flag with explicit pointer to the existing `cinder::InMemoryTieringStore` API which accepts arbitrary `SystemTime`. The overwrite-semantics-no-special-case decision (D-Overwrite) is justified against the alternative `--no-overwrite` / pre-flight `get_entry` check with explicit pointer to the task brief's out-of-scope item. | PASS |
| 2.1 Missing stakeholder perspectives | Primary: platform operator (Priya). Secondary: Cinder maintainer + Lumen maintainer (covered by the meta-AC — the existing locked test files for the eight predecessor features continue to pass green UNMODIFIED, so the extension does not perturb any shipped feature). Tertiary: scripting/automation consumers (covered by the explicit operator-friendly stdout/stderr contracts: one literal line on success, one stderr line containing the offending value on failure, non-zero exit) + observability sidecar consumers (covered by OK4 — one `cinder.place.count` line per call with the established byte format that downstream OTLP collectors already consume from `ingest --observe-otlp` and `migrate --observe-otlp`). | PASS |
| 2.2 Missing error scenarios | One distinct CLI error scenario specified: invalid-tier (OK3 — parse-side error before the place call). The underlying `TieringStore::place` API returns `()` (no failure modes at the trait level per `crates/cinder/src/store.rs:78-81`); Cinder open errors are surfaced via the existing `kaleidoscope_cli::Error::CinderOpen(MigrateError)` variant (`crates/kaleidoscope-cli/src/lib.rs:77`) and bubble to the binary's existing error printer in `main()`; `--observe-otlp` file-open errors are surfaced via the existing `Error::Io(std::io::Error)` variant (line 82). No new error variant introduced. The overwrite-semantics case (OK2) is technically a "success" path but is documented as the exhaustive opposite of "AlreadyPlaced" rejection — proving the CLI does NOT introduce a special case that would diverge from the trait contract. |
| 2.3 Missing NFRs | NFRs covered: place-success correctness (OK1 — operator-facing correctness invariant), overwrite-semantics faithfulness (OK2 — guardrail against introducing a special-case CLI guard), fail-fast on invalid tier (OK3 — operator-facing fail-fast guarantee), observability emission (OK4 — operator-facing OTLP-JSON sidecar contract), no-Lumen-touch invariant (the Lumen WAL+snapshot is byte-equivalent before and after every call), tenant-isolation invariant (inherited from `cinder::TieringStore`). Performance: the v0 contract is "one `place` call per invocation" which is whatever the existing trait method returns; acceptable at v0 tier sizes. | PASS |
| 3.1 Vague performance requirements | All "100%"/"0%" claims are quantitative and falsifiable via the named test. No "fast", "scalable", "performant" adjectives without a number. The "in milliseconds" claim in the Elevator Pitch is descriptive operator language inherited from the eight predecessor features, not a normative requirement; the AC do not quantify a latency target. | PASS |
| 3.2 Ambiguous requirements | Every AC pins exact string literals (`placed tenant=`, `item=`, `tier=`, the `\n` line terminator), exit code values (0 for success, non-zero for failure), substring assertions on stderr (the verbatim invalid value for OK3), substring assertions on the OTLP-JSON file content (`cinder.place.count`, tenant id, lower-case tier for OK4), exact line count on the OTLP-JSON file (one per place call), or state-level equality / inequality on `get_entry().tier`. | PASS |
| 4 Testability | Every AC is a property of (a) the captured stdout bytes after the place function returns, (b) the captured stderr bytes, (c) the returned `Result`, (d) a subsequent `cinder.get_entry(tenant, item)` call against a freshly-opened Cinder store, (e) the existence and line content of the optional `--observe-otlp` sidecar file, or (f) the presence/byte-content of the `lumen_base(data_dir)` directory. Every AC is automatable via `String::from_utf8` + `Vec<u8>` byte comparison + `str::contains` substring matching + `assert_eq!` integer / enum comparison + filesystem checksum or existence assertion + `std::fs::read_to_string` line-count assertion. The OK2 overwrite-semantics is doubly testable: directly via the new test file's overwrite scenario AND indirectly via the absence of an "AlreadyPlaced" branch in the library function (DESIGN MUST NOT introduce one). | PASS |
| 5.1 Largest bottleneck | The largest remaining gap in the operator's daily Cinder workflow is "I want to record a Cinder placement DIRECTLY (without running `ingest` first), but the only way today is to write a Rust harness against `cinder::FileBackedTieringStore::place` or fabricate NDJSON records and run `ingest`". This feature addresses exactly that gap by adding the missing CLI surface — the CRUD "C" that the eight predecessor features have not yet shipped. After this ships, `ingest` / `place` / `read` / `stats` / `list-items` / `migrate` cover the operator's full Cinder lifecycle surface (Create x2, Read x3, Update, Delete is still missing — a future feature). | PASS |
| 5.2 Simpler alternatives considered | Considered and rejected: (a) bulk placement shape (`place <tenant> <data_dir> <item_ids_file> <tier>`) — rejected in `wave-decisions.md` D-OutOfScope-Bulk, single-item is the v0 primitive; (b) pre-flight existence check / `--no-overwrite` flag — rejected in D-Overwrite / D-OutOfScope-ExistsCheck per the task brief's explicit out-of-scope, the underlying API is overwrite-semantics by design; (c) deterministic-time flag (`--placed-at <timestamp>`) — rejected in D-Timestamp / D-OutOfScope-PlacedAt, `SystemTime::now()` is the only value a non-test caller needs; (d) mixed-case tier argument (`Hot`/`HOT`) — rejected in D-LowerCase, breaks symmetry with the established parse-side convention; (e) a new `TieringStore::place_if_absent(...) -> Result<(), AlreadyPlaced>` trait method — not introduced in v0 per the task brief's explicit out-of-scope, the read-then-write pattern via the existing `place` is sufficient at v0 use cases. | PASS |
| 5.3 Constraint prioritization | The dominant constraint is correctness (OK1 — the report content must match the request AND the post-call `get_entry().tier` must equal the requested `tier`). OK2 (overwrite-semantics fidelity) is correctly weighted as the guardrail against introducing a special-case CLI guard that would diverge from the underlying API. OK3 (invalid-tier fail-fast) is correctly weighted as the parse-side error path. OK4 (`--observe-otlp` emission) is correctly weighted as the operator-facing observability surface. There is no cross-process or concurrency invariant to prioritize because the extension has only one write (`place`) per invocation and runs in a single thread. | PASS |
| 5.4 Data-justified | This is not a performance optimization. The decision to use one `place` call per invocation (vs introducing a `place_if_absent` trait method) is justified by the v0 scope (under-1-day effort, no new trait methods) and by the task brief's explicit out-of-scope item ("Verification that item_id doesn't already exist (place() is overwrite-semantics per Cinder API — document)"). The decision to hard-wire `SystemTime::now()` (vs introducing a `--placed-at` flag) is justified by the existing in-memory adapter accepting arbitrary `SystemTime` for unit tests at the trait level. The decision to ship the single-item primitive (vs bulk placement) is justified by the standard "v0 ships the primitive, v1 ships ergonomic batching" pattern. The decision to inherit the existing `--observe-otlp` flag convention (vs introducing a new flag name or output format) is justified by the operator's muscle memory across `ingest --observe-otlp`, `read --observe-otlp`, `migrate --observe-otlp`. | PASS |

## DoR Status: PASSED — with four honestly-recorded artefact gaps

The story-level DoR (9 items × 1 story = 9 checks) is PASSED with
evidence on every item. The feature-level DoR is PASSED with the
following honestly-recorded artefact gaps (inherited posture from
the eight predecessor features):

- **F1, F2 (journey artefacts)**: deliberately not produced. The
  journey is one step (operator runs `place <tenant> <data_dir>
  <item_id> <tier> [--observe-otlp <path>]` and reads stdout); a
  multi-step journey visual or YAML would duplicate the Elevator
  Pitch in US-01. The eight reference features' journey visuals
  collectively cover the underlying CLI operator mental model.
  Acknowledged; will not be remediated.
- **F3 (Gherkin `.feature` file)**: deliberately not produced.
  Per the project convention, this project's acceptance idiom is
  Rust `#[test]` functions with `// Given / // When / // Then`
  comment blocks. The Given/When/Then specification lives inline
  in `user-stories.md`.
- **F4 (shared artefact registry)**: not produced. The
  cross-feature shared artefacts are tracked in the eight
  reference features and the source code itself; the
  output-shape contracts are restated as System Constraints in
  `user-stories.md`. Producing a new registry that copies the
  reference features' rows would add no value.
- **F6 (separate `prioritization.md`)**: not produced; integrated
  into `story-map.md`'s `## Priority Rationale` section per the
  `nw-leanux-methodology` skill template, because a one-slice
  feature does not earn a separate prioritization document.

Anti-pattern scan clean. Dimension 0 elevator-pitch check PASSES
with the real entry point (`kaleidoscope-cli place acme /tmp/data
acme/bootstrap-00001 hot` — a NEW subcommand on the existing
binary) and the concrete byte-level output (one literal stdout
line on success; non-empty stderr line with the offending value
on the invalid-tier failure path; one `cinder.place.count` line
per call when `--observe-otlp` is set). Dimensions 1-5
confirmation-bias self-check PASSES.

The DISCUSS wave is ready to hand off to `nw-solution-architect`
for DESIGN.
