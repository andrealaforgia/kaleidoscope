target_environments:
  - name: clean
    description: "CI workflow addition; runs on GitHub Actions ubuntu-latest"
    platform: [linux]
    preconditions: []
coexistence_matrix: []
platform_coverage:
  CI: [GitHub Actions ubuntu-latest]
deployment_assumptions:
  - "Workflow YAML edit is additive; sibling jobs unchanged"
  - "cargo-mutants tool already installed by sibling jobs"
