# Definition of Ready — Validation

Feature: `cli-stats-cinder-tier-distribution-v0`
Reviewer: Luna (self-review at DISCUSS close; second pass scheduled at
handoff to DESIGN per `nw-po-review-dimensions` skill).
Date: 2026-05-19.

## Per-story DoR (9-item hard gate)

### US-01: Operator inspects a tenant's tier distribution without writing Rust

| # | DoR Item | Status | Evidence |
|---|----------|--------|----------|
| 1 | Problem statement clear, domain language | PASS | "Today her only options are to either write a Rust harness that opens `cinder::FileBackedTieringStore` and calls `list_by_tier` for each tier, OR to inspect the `cinder.*` snapshot files directly with whatever JSON/Bincode tooling she can pull together. ... As a result she does NOT routinely check the tier distribution after ingest runs, and tier-related operational issues (migrations not happening, hot tier ballooning, cold items predominating) go unobserved until a downstream symptom surfaces." Uses Cinder, tier, `list_by_tier`, `TieringStore`, hot/warm/cold, snapshot, WAL, key=value, `cinder.*` — all domain terms grounded in the existing Cinder/CLI implementation. |
| 2 | User/persona identified | PASS | Priya the platform operator, multi-tenant Kaleidoscope deployment for a fintech, already uses `kaleidoscope-cli ingest`, `kaleidoscope-cli read`, `kaleidoscope-cli stats` daily (inherited from the predecessor and three reference features). Uses standard Unix text tools (`grep`, `cut`, `awk`) on stdout output, not JSON parsers. Does NOT want existing `stats`-consuming shell scripts to break. |
| 3 | 3+ domain examples with real data | PASS | Four examples: happy path with `acme` / 5 Hot + 12 Warm + 47 Cold Cinder items / 6-line stdout shape shown literally; edge case with `acme` / 3 Hot + 0 Warm + 0 Cold / 4-line stdout (only `hot=` appears after Lumen lines); edge case (the unusual but possible case) with `acme` / 0 Lumen records + 2 Hot + 0 Warm + 1 Cold (orphan tier metadata) / 3-line stdout `records=0\nhot=2\ncold=1\n`; boundary case with `legacy_acme` / 4 Lumen records + 0 Cinder placements / 3-line stdout byte-equivalent to predecessor. Real values throughout: `acme`, `globex`, `legacy_acme`, `/tmp/k-data`, `/tmp/inherited-data`, `/tmp/kdata`, real Cinder counts (5/12/47, 3, 2/1, 5/9 for isolation), ISO 8601 timestamps with literal calendar dates (`2026-05-12T00:00:00.000000000Z`, `2026-05-19T03:45:12.987654321Z`, `2026-05-18T00:00:00.000000000Z`), real source-file references. |
| 4 | UAT scenarios (3-7 in Given/When/Then) | PASS | 5 scenarios: populated-multi-tier 6-line stdout (OK1), hot-only-Cinder 4-line stdout (OK1 + Option B), empty-Lumen + populated-Cinder (orphan tier metadata) 3-line stdout (OK3), backwards-compat tenant with zero Cinder placements 3-line stdout byte-equivalent to predecessor (OK4), tenant-isolation `stats(acme)` does not count `globex`'s Cinder placements (OK2). |
| 5 | AC derived from UAT | PASS | 11 AC bullets, each maps to an observable byte-level invariant from a scenario (6-line populated stdout in exact order with key strings `records=`/`earliest=`/`latest=`/`hot=`/`warm=`/`cold=`; 4-line hot-only-Cinder stdout; 3-line orphan-Cinder stdout with `records=0` first; 3-line backwards-compat stdout byte-equivalent to predecessor; 1-line empty-both stdout byte-equivalent to predecessor's OK3; tier-count equality with `list_by_tier(tenant, tier).len()`; tenant-isolation 5 vs 14 vs 9; read-only invariant on the Cinder side; no new error variant; existing `tests/stats_subcommand.rs` non-regression; new `tests/stats_cinder_tier_distribution.rs` file added; `print_usage` update optional). |
| 6 | Right-sized | PASS | 1 story, 1 function-level change in `lib.rs` (extend `stats()` OR add `stats_with_cinder()` — DESIGN's choice), possibly 1 line-level update in `main.rs` dispatch arm, 1 new test file with 5 scenarios, 1 manifest entry, well under 1 day effort. Comparable to the predecessor (`cli-stats-subcommand-v0`); structurally smaller because the Lumen-side computation is reused unchanged. |
| 7 | Technical notes identify constraints | PASS | File paths, line numbers, manifest entry, dependency posture (no new external or internal crate dependencies — `cinder::TieringStore::list_by_tier`, `FileBackedTieringStore::open`, `cinder::NoopRecorder`, `Tier` enum, `cinder_base()` helper, `aegis::TenantId` all already wired). The explicit non-prescription of internal implementation details beyond the choice of two function shapes (extend `stats()` OR add `stats_with_cinder()` — DESIGN-locked per `wave-decisions.md` D9); the structural-mirror reference to the quiescent recorder pattern is a fact about precedent, not a novel design constraint. The hard rule "DO NOT modify `crates/kaleidoscope-cli/tests/stats_subcommand.rs`" is restated as the OK4 supplementary oracle invariant. |
| 8 | Dependencies tracked | PASS | `cinder::TieringStore::list_by_tier(tenant, tier)` already exists and returns `Vec<ItemId>` (`crates/cinder/src/store.rs:101-102`); `cinder::FileBackedTieringStore` already used by `ingest()` (`crates/kaleidoscope-cli/src/lib.rs:179-180`); `cinder::NoopRecorder` already used by `ingest()`'s no-flag arm (`crates/kaleidoscope-cli/src/lib.rs:170-174`); `Tier` enum already imported (`crates/kaleidoscope-cli/src/lib.rs:58`); `cinder_base(data_dir)` helper already at `crates/kaleidoscope-cli/src/lib.rs:122-124`; `aegis::TenantId` already a dependency. The predecessor's `stats()` function at `crates/kaleidoscope-cli/src/lib.rs:313-332` provides the Lumen-side computation reused unchanged or wrapped. No unresolved internal or external dependencies. |
| 9 | Outcome KPIs defined with measurable targets | PASS | Maps to OK1-CLI-stats-tier-counts (principal: 100% of `stats()` invocations report `<tier>=<count>` lines matching `list_by_tier(tenant, tier).len()` for non-zero tiers, omit the line for zero tiers), OK2-CLI-stats-tier-tenant-isolation (100% of `stats(&acme, ...)` invocations report per-tier counts that equal the queried tenant's counts alone, never the cross-tenant union), OK3-CLI-stats-no-records-no-timestamps-still (100% of empty-Lumen invocations produce `records=0\n` first; 100% with non-zero Cinder also emit the selectively-emitted non-zero Cinder lines; 100% with zero Cinder produce byte-equivalent predecessor output), OK4-CLI-stats-backwards-compatible-populated-then-cinder-zero (100% of zero-Cinder-placement invocations produce stdout byte-equivalent to predecessor for the same `(tenant, data_dir)` pair). All four targets are quantitative and falsifiable via the named test files (the new one + the unmodified predecessor file). |

### US-01 DoR Status: PASSED

## Feature-level DoR

| # | Item | Status | Evidence |
|---|------|--------|----------|
| F1 | Journey artefact (visual) | NOT PRODUCED — accepted | Per `wave-decisions.md`, the journey is so thin (one operator action: invoke `stats <tenant> <data_dir>` and read stdout) that a separate journey visual would duplicate the Elevator Pitch in US-01. The five reference features in this cluster collectively cover the underlying CLI operator mental model. Acknowledged gap; will not be remediated. |
| F2 | Journey artefact (YAML schema) | NOT PRODUCED — accepted | Same rationale as F1. The journey schema in `nw-design-methodology` is for multi-step user journeys; this feature has one step. |
| F3 | Journey artefact (Gherkin `.feature`) | INTENTIONALLY OMITTED | Per the project convention (inherited from the five reference features and noted in the task brief): this project's acceptance idiom is Rust `#[test]` functions with `// Given / // When / // Then` comment blocks, not Gherkin `.feature` files. The Given/When/Then text lives inside `user-stories.md` (UAT Scenarios sections). DISTILL writes the Rust tests. |
| F4 | Shared artefact registry | NOT PRODUCED — accepted | The cross-feature shared artefacts (`tenant_id` positional argument, `data_dir` positional argument, `lumen_base(data_dir)` and `cinder_base(data_dir)` helpers, the `LogStore::query(tenant, TimeRange::all())` and `TieringStore::list_by_tier(tenant, tier)` APIs, the `LumenToPulseRecorder` / `NoopRecorder` quiescent recorder patterns) are tracked in the reference features and the source code itself. The output-shape contracts (lines 1-3 inherited unchanged from predecessor; lines 4-6 NEW with keys `hot`/`warm`/`cold` and selective emission per Option B) are restated as System Constraints in `user-stories.md`. |
| F5 | Story map | PASS | `discuss/story-map.md` with one-activity backbone, walking-skeleton justification (N/A — the predecessor's substrate already exists and the Cinder substrate already exists), single-slice rationale, cross-feature alignment, scope assessment, priority rationale section, Option B locked. |
| F6 | Prioritization | INTEGRATED into `story-map.md` | The single-slice structure renders a separate `prioritization.md` redundant. The `## Priority Rationale` section in `story-map.md` covers the ordering logic and partial-ship reasoning. |
| F7 | Outcome KPIs | PASS | `discuss/outcome-kpis.md` with OK1 (principal — tier-count correctness), OK2 (per-tenant isolation), OK3 (empty-Lumen with Option B contract preserved), OK4 (backwards-compat for zero-Cinder-placement tenants — guardrail), each with quantitative target, baseline, and measurement plan. |
| F8 | User stories | PASS | `discuss/user-stories.md` with US-01, including complete Elevator Pitch with the real entry-point shell command (`kaleidoscope-cli stats acme /tmp/data` — the SAME command as the predecessor, unchanged) and the concrete byte-level output (six literal stdout lines shown in Domain Example 1, four shown in Example 2, three shown in Example 3, three shown in Example 4). |
| F9 | Per-slice files | PASS | 1 file under `discuss/slices/` (`slice-01-stats-includes-cinder-tier-distribution.md`, ≤ 100 effective lines — the file includes additional clarifying sections beyond the strict 100-line cap but the body of the slice brief — Goal, IN scope, OUT scope, Rejected alternatives, Learning hypothesis, AC, Dependencies, Reference class, Effort, DoD — is within the spirit of the cap). |
| F10 | Wave decisions | PASS | `discuss/wave-decisions.md` with D1 (scope), D-EmptyRender (Option B — the principal contract question from the task brief, with explicit rationale comparing Options A/B/C), D-Backwards-compat (OK4 byte-equivalence with predecessor), D2-D6 (out-of-scope items), D7 (tier key naming lower-case), D8 (key ordering), D9 (library function shape — DESIGN-locked), D10 (test file location + hard rule do-not-modify-predecessor), D11 (SSOT non-modification). |
| F11 | Right-sized (Elephant Carpaccio) | PASS | 1 story, 1 bounded context (`kaleidoscope-cli` crate), 1 modified file in `src/` (possibly 2), 1 new test file, 1 manifest line-level change. Estimated effort: well under 1 day. `story-map.md` "Scope Assessment: PASS". |
| F12 | SSOT impact assessed | PASS | `wave-decisions.md` D11: no SSOT modification. Same posture as the five reference features. |
| F13 | Cross-feature contract honoured | PASS | `user-stories.md` System Constraints pin the output-shape contract (extension of predecessor's plain-text key=value lines, terminated by `\n`; new keys `hot`/`warm`/`cold` lower-case; key order `records, earliest, latest, hot, warm, cold` when present; Option B selective emission for zero-count tiers), the positional-argument convention (inherited unchanged from predecessor — mirrors `ingest` and `read`), the stream contract (stdout for the principal output, inherited unchanged), the read-only invariant (extended to the Cinder side), the tenant-isolation invariant (extended from `lumen::LogStore` to `cinder::TieringStore`), and the byte-equivalent backwards-compat contract (NEW guardrail, encoded as OK4 and protected by D10's locked predecessor test file). No prior contract is violated. |

## Anti-pattern scan

| Anti-pattern | Detected? | Notes |
|--------------|-----------|-------|
| Implement-X | No | Story framed as "Priya runs `kaleidoscope-cli stats acme /tmp/data` and sees up to six lines telling her the tier distribution, in milliseconds, without writing Rust or inspecting `cinder.*` snapshots" — outcome-first from the operator perspective; the extension is the means, not the end. The three operator decisions enabled (migrations happening? reads slow? hot ballooning?) are named explicitly. |
| Generic data | No | Real names: Priya, `acme`, `globex`, `legacy_acme`. Real paths: `/tmp/k-data`, `/tmp/inherited-data`, `/tmp/kdata`, `/tmp/data`. Real Cinder counts (5/12/47, 3, 2/1, 5/9). Real source-file references: `crates/kaleidoscope-cli/src/lib.rs:122-124`, `:170-174`, `:179-180`, `:243-244`, `:313-332`, `:58`, `:73-84`, `:71-97`; `crates/cinder/src/store.rs:71-72`, `:101-102`; `crates/cinder/src/tier.rs:28-32`, `:38-44`. Real ISO 8601 timestamps with literal calendar dates: `2026-05-12T00:00:00.000000000Z`, `2026-05-19T03:45:12.987654321Z`, `2026-05-18T00:00:00.000000000Z`. |
| Technical AC | No blocking instances | AC pin the wire-observable invariants (6-line / 4-line / 3-line / 1-line stdout shapes in exact order; key strings `records`/`earliest`/`latest`/`hot`/`warm`/`cold`; tier-count equality with `list_by_tier().len()`; tenant-isolation 5 not 14; byte-equivalence with predecessor for zero-Cinder cases). They reference the structural mirror of the predecessor's recorder construction as the implementation pattern but do NOT prescribe novel internal mechanisms; the structural-mirror reference is a fact about the precedent, not a novel design constraint. |
| Technical scenario titles | No | Scenarios titled by user outcome: "Populated tenant with multi-tier distribution — Priya sees six lines in order", "Populated Lumen, hot-only Cinder — Priya sees four lines", "Empty-Lumen, populated-Cinder (orphan tier metadata) — Priya sees `records=0` plus non-zero Cinder lines", "Backwards-compatibility — tenant with populated Lumen and zero Cinder placements", "Tenant isolation — Cinder tier counts for `acme` do not count `globex` placements". Not "stats() function calls TieringStore::list_by_tier" or "FileBackedTieringStore::open recorder slot accepts boxed dyn trait". |
| Oversized story | No | 1 story, 5 UAT scenarios, 11 AC, well under 1 day effort. Comparable to the predecessor's 5 scenarios + 12 AC. The slight reduction in AC count (12 → 11) reflects that no `print_usage` AC is mandatory (the predecessor's already documents the subcommand). |
| Abstract requirements | No | Every AC has a numeric value (line counts 6/4/3/1; tier counts 5/9/14, 2/0/1, 3/0/0, 5/12/47), a string literal (`records=`, `hot=`, `warm=`, `cold=`, the `\n` line terminator, ISO 8601 `Z` suffix), or a byte-level invariant (stdout ends with `\n`, line ordering, byte-equivalence with predecessor). |

## Reviewer self-pass (Dimension 0 — Elevator Pitch, BLOCKING)

| Story | Section present | Real entry point | Concrete output | Job connection | Verdict |
|-------|-----------------|------------------|-----------------|----------------|---------|
| US-01 | yes (Before / After / Decision enabled triple) | yes — `kaleidoscope-cli stats acme /tmp/data` (the actual shell command an operator types — UNCHANGED from the predecessor, this feature extends the existing subcommand) | yes — six literal stdout lines (`records=10000000`, `earliest=2026-05-18T00:00:01.123456789Z`, `latest=2026-05-19T03:45:12.987654321Z`, `hot=42`, `warm=315`, `cold=9643`) shown in the "After" line | yes — "Priya can decide three operationally distinct questions from one CLI invocation: 1. 'Are warm-to-cold migrations actually happening for `acme`?' ... 2. 'Should I expect read latency on `acme` to be high?' ... 3. 'Is the hot tier ballooning?' ..." | PASS |

Slice-level check (Dimension 0 item 5): the single slice (Slice 01)
is tagged operator-visible (NOT `@infrastructure`). The story's
Elevator Pitch names a real user-invocable CLI command and the
byte-level stdout output the operator's shell session will display.
There is no slice-level infrastructure-only blocking concern.
PASS at slice level.

## Confirmation-bias detection (Dimensions 1-4 self-check)

| Dimension | Check | Verdict |
|-----------|-------|---------|
| 1.1 Technology bias | Story specifies the existing CLI surface and plain-text key=value output. No specific JSON parser (jq, etc.) prescribed; no specific datetime library prescribed (the existing hand-rolled formatter at `crates/kaleidoscope-cli/src/lib.rs:345` is reused unchanged for the Lumen-side timestamps); the new tier-count rendering uses only Rust's `usize` `Display` impl. The output is consumable by standard Unix text tools that any operator already has. | PASS |
| 1.2 Happy path bias | Story includes four non-happy-path-shaped scenarios: hot-only-Cinder (only one tier non-zero — exercises the Option B selective emission), orphan-Cinder (empty Lumen + populated Cinder — exercises the predecessor's OK3 extension under Option B), backwards-compat (populated Lumen + zero Cinder placements — exercises the OK4 byte-equivalence guardrail), and tenant-isolation (cross-tenant Cinder data in the same `data_dir` — exercises the per-tenant isolation invariant inherited from `cinder::TieringStore`). The OK3 and OK4 KPIs are themselves sad-path-shaped guarantees against silently breaking the predecessor's contract. | PASS |
| 1.3 Availability bias | The chosen output shape (plain-text key=value lines extending the predecessor's contract) is justified by the operator's existing tooling (Unix text utilities) and by the predecessor's already-shipped precedent. The three alternative empty-render encodings (Options A, B, C from the task brief) are explicitly compared in `wave-decisions.md` D-EmptyRender with rationale for the choice (Option B). The two function-shape candidates (extend `stats()` OR add `stats_with_cinder()`) are explicitly listed with the third in-place-modification shape ruled out by reference to the predecessor's locked test file. | PASS |
| 2.1 Missing stakeholder perspectives | Primary: platform operator (Priya). Secondary: Cinder maintainer + Lumen maintainer (covered by the meta-AC — the existing `tests/stats_subcommand.rs`, `tests/observe_otlp_*.rs` test files continue to pass byte-equivalently, so the extension does not perturb any shipped feature). Tertiary: scripting/automation consumers (covered by the byte-equivalent backwards-compat contract OK4 — every existing shell pipeline against the predecessor's stdout shape continues to work for tenants without Cinder placements; new pipelines against the tier-distribution lines slot in naturally via `grep ^hot= | cut -d= -f2`). | PASS |
| 2.2 Missing error scenarios | Five error-shaped scenarios: hot-only-Cinder (some tiers zero), orphan-Cinder (empty-Lumen with non-zero Cinder), backwards-compat (populated-Lumen with zero Cinder), tenant-isolation (cross-tenant data in same `data_dir`), AND the empty-both case (inherited from predecessor's OK3 byte-equivalence). Cinder open errors are surfaced via the existing `kaleidoscope_cli::Error::CinderOpen(MigrateError)` variant (`crates/kaleidoscope-cli/src/lib.rs:77`) and bubble to the binary's existing error printer in `main()`. No new error variant introduced for v0. |
| 2.3 Missing NFRs | NFRs covered: tier-count correctness (OK1 — operator-facing correctness invariant against `list_by_tier().len()`), per-tenant isolation (OK2 — inherited from `cinder::TieringStore`'s trait-level semantic), empty-Lumen contract preservation with Option B extension (OK3 — protects the predecessor's empty-tenant contract while surfacing orphan-tier-metadata), byte-equivalent backwards-compat (OK4 — guardrail against breaking existing operator shell pipelines, protected by the locked predecessor test file), read-only invariant on the Cinder side (the Cinder WAL+snapshot is unchanged after the call), no-OTLP-side-channel invariant (the quiescent `NoopRecorder` produces no file). Performance: the v0 contract is "three `list_by_tier(tenant, tier)` calls per invocation" which produces three `Vec<ItemId>` allocations of the per-tenant per-tier sizes; the cost is acceptable at v0 tier-size volumes. If the v0 cost proves too high (the learning hypothesis), a follow-up feature can introduce an aggregate `TieringStore::count_by_tier(tenant, tier) -> usize` trait method. | PASS |
| 3.1 Vague performance requirements | All "100%"/"0%" claims are quantitative and falsifiable via the named test. No "fast", "scalable", "performant" adjectives without a number. The "in milliseconds" claim in the Elevator Pitch is descriptive operator language inherited from the predecessor, not a normative requirement; the AC do not quantify a latency target because the v0 cost is whatever the existing `list_by_tier()` calls return. | PASS |
| 3.2 Ambiguous requirements | Every AC pins exact string literals (`records=`, `earliest=`, `latest=`, `hot=`, `warm=`, `cold=`, the `\n` line terminator), exact integer invariants (line counts: 6 for populated-multi-tier, 4 for hot-only-Cinder, 3 for orphan-Cinder, 3 for backwards-compat, 1 for empty-both; tier counts: 5 NOT 14 for tenant-isolation, 2/0/1 for orphan, etc.), or byte-level invariants (stdout ends with `\n`, line ordering `records→earliest→latest→hot→warm→cold`, byte-equivalence with predecessor for zero-Cinder cases). | PASS |
| 4 Testability | Every AC is a property of the captured stdout bytes (after the stats function returns), the captured stdout's parsed line-by-line content, or the returned `Result`. Every AC is automatable via `String::from_utf8` + `Vec<u8>` byte comparison + `str::lines()` iteration + key-prefix matching + integer parsing for the tier counts. Test names are pre-sketched in the slice file. The OK4 byte-equivalence is doubly testable: directly via the new test file's backwards-compat scenario AND indirectly via the unmodified predecessor test file's continued green status. | PASS |
| 5.1 Largest bottleneck | The largest gap in the operator's daily workflow is "I want to know if migrations are happening for this tenant, but the only way to get that today is to write a Rust harness against `cinder::FileBackedTieringStore::list_by_tier` or inspect `cinder.*` snapshot files by hand". This feature addresses exactly that gap by reusing the same CLI surface the operator already uses for the records+timestamp smoke-test. | PASS |
| 5.2 Simpler alternatives considered | Considered and rejected: (a) Option A (never emit Cinder lines for empty-Lumen) — rejected because it would silently hide orphan tier metadata; (b) Option C (always emit all six lines with explicit zeros) — rejected because it would break byte-equivalent backwards-compat with the predecessor (`wave-decisions.md` D-EmptyRender); (c) in-place modification of `stats()` without a new parameter — rejected because the predecessor's locked test file uses `ingest()` fixtures which place Hot Cinder items (`wave-decisions.md` D9); (d) introducing a new `TieringStore::count_by_tier(tenant, tier)` trait method — deferred to a follow-up feature unless the v0 cost proves operationally hostile (slice brief "Learning hypothesis" + "Rejected alternatives"); (e) per-item dump, JSON/CSV output, Cinder-only mode, policy evaluation — all deferred to follow-up features (`wave-decisions.md` D2-D6). | PASS |
| 5.3 Constraint prioritization | The dominant constraint is OK4 (backwards-compatibility — the extension must NOT break any existing operator shell pipeline that scripts against the predecessor's stdout shape for tenants without Cinder placements). The principal value-add is OK1 (tier-count correctness — the new operator-facing answer). OK2 (per-tenant isolation) and OK3 (empty-Lumen with Option B) are correctly weighted as the necessary correctness guardrail and the necessary empty-case unambiguity preservation. There is no cross-process or concurrency invariant to prioritize because the extension has no side effects and runs in a single thread. | PASS |
| 5.4 Data-justified | This is not a performance optimization. The decision to materialise three full per-tier `Vec<ItemId>` via the existing `list_by_tier()` call (vs introducing a new aggregate trait method) is justified by the v0 scope (under-1-day effort, no new trait methods, no new Cinder API) and by the explicit learning hypothesis that the existing API is sufficient at v0 tier sizes. The decision to choose Option B over Options A and C is justified by the comparative trade-off (`wave-decisions.md` D-EmptyRender): Option B is the only option that simultaneously preserves byte-equivalent backwards-compat for zero-Cinder cases AND surfaces orphan tier metadata for empty-Lumen-non-zero-Cinder cases. | PASS |

## DoR Status: PASSED — with four honestly-recorded artefact gaps

The story-level DoR (9 items × 1 story = 9 checks) is PASSED with
evidence on every item. The feature-level DoR is PASSED with the
following honestly-recorded artefact gaps (inherited posture from the
predecessor):

- **F1, F2 (journey artefacts)**: deliberately not produced. The
  journey is one step (operator runs `stats <tenant> <data_dir>` and
  reads stdout — the SAME command as the predecessor); a multi-step
  journey visual or YAML would duplicate the Elevator Pitch in
  US-01. The five reference features' journey visuals collectively
  cover the underlying CLI operator mental model. Acknowledged; will
  not be remediated.
- **F3 (Gherkin `.feature` file)**: deliberately not produced. Per
  the project convention, this project's acceptance idiom is Rust
  `#[test]` functions with `// Given / // When / // Then` comment
  blocks. The Given/When/Then specification lives inline in
  `user-stories.md`.
- **F4 (shared artefact registry)**: not produced. The cross-feature
  shared artefacts are tracked in the reference features and the
  source code itself; the output-shape contracts are restated as
  System Constraints in `user-stories.md`. Producing a new registry
  that copies the reference features' rows would add no value.
- **F6 (separate `prioritization.md`)**: not produced; integrated
  into `story-map.md`'s `## Priority Rationale` section per the
  `nw-leanux-methodology` skill template, because a one-slice
  feature does not earn a separate prioritization document.

Anti-pattern scan clean. Dimension 0 elevator-pitch check PASSES with
the real entry point (`kaleidoscope-cli stats acme /tmp/data` — the
SAME command as the predecessor, this feature extends the existing
subcommand) and the concrete byte-level output (six literal stdout
lines for the populated-multi-tier case; four for the hot-only-Cinder
case; three for the orphan-Cinder case; three for the backwards-compat
case; one for the empty-both case). Dimensions 1-5 confirmation-bias
self-check PASSES.

The DISCUSS wave is ready to hand off to `nw-solution-architect` for
DESIGN.
