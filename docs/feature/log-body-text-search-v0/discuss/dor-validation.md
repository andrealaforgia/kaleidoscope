# Definition of Ready: log-body-text-search-v0

The 9-item DoR gate per the LeanUX template. All evidence is grounded in
artefacts inside this directory and source files inside this repository.

## Story: log-body-text-search-v0 (US-01..US-06, single thin slice)

| # | DoR item | Status | Evidence |
|---|---|---|---|
| 1 | Problem statement clear, domain language | PASS | `user-stories.md` US-01 § Problem names Sara Mendez, the 240-record incident-window breakdown (198 unrelated INFO/WARN, 40 unrelated ERROR from a separate checkout-timeout incident, 2 ERROR records whose `body` is `"kafka timeout connecting to broker-3"` and `"kafka timeout after 30s on topic orders"`), and the `jq 'map(select(.body \| contains(...)))'` workaround. Domain language throughout: `LogRecord.body`, `Predicate`, `query_with`, `body_contains`, `min_severity`, `query-http-common`, `acme-prod` tenant id, `MAX_RESULT_ROWS`. |
| 2 | User/persona with specific characteristics | PASS | `user-stories.md` US-01 § Who names three personas with concrete characteristics: Sara Mendez (SRE on `acme-prod`, mid-incident, terminal + curl + jq, triage urgency), Marcus Webb (platform engineer, automated incident classifier polling every 60s, throughput motive), Priya Raman (support engineer, customer-ticket triage, correctness motive). Each persona has a distinct motive and a distinct surface against the new parameter. |
| 3 | 3+ domain examples with real data | PASS | `user-stories.md` US-01 § Domain Examples carries SIX examples (happy path with `kafka timeout`, calm-empty with `cassandra`, default unchanged with no parameter, empty rejected with `?body_contains=`, case-sensitive with `KAFKA` vs `kafka`, cross-tenant isolation with `acme-prod` vs `globex-staging`). Each example carries real tenant ids (`acme-prod`, `globex-staging`), real timestamps (`1716200005` ... `1716200030` epoch seconds, `1_716_200_005_000_000_000` ... `1_716_200_030_000_000_000` nanoseconds), real severity ladder values (9 INFO, 17 ERROR), and concrete URL fragments. |
| 4 | UAT in Given/When/Then (3-7 scenarios) | PASS | Six Gherkin scenarios in `user-stories.md` § UAT Scenarios (within the 3-7 range): "A known substring narrows the response to matching records", "An unmatched substring returns the calm empty array, never 404", "Parameter absent returns every record in the window", "An empty body_contains value is a redacted 400", "The match is case-sensitive (KAFKA does not match kafka)", "Cross-tenant isolation". Each scenario has concrete data tables and observable outcomes phrased in Given/When/Then form. |
| 5 | AC derived from UAT | PASS | `user-stories.md` § Acceptance Criteria (under US-01) carries 13 checkboxes, each traceable to one or more scenarios. The optional-parameter AC traces to scenarios 1 and 3; the substring-match AC traces to scenarios 1 and 6; the empty-value AC traces to scenario 4; the redaction AC traces to scenario 4; the case-sensitivity AC traces to scenario 5; the calm-empty AC traces to scenario 2; the cross-tenant AC traces to scenario 6; the composition-with-min-severity AC traces to the System Constraints section and is exercisable by extending scenarios 1 or 2; the cap and envelope ACs trace to System Constraints; the lumen-byte-identical AC traces to Technical Notes and FLAG 3; the `query-http-common`-sole-provider AC traces to KPI-3 in `outcome-kpis.md`; the under-30-LOC AC traces to KPI-3. |
| 6 | Right-sized (1-3 days, 3-7 scenarios) | PASS | 6 stories, 6 scenarios, 1 day estimated. `story-map.md` § "Scope Assessment" explicitly passes the carpaccio gate: 6 user stories (under 10), 1 primary bounded context (`crates/log-query-api`) with one INCIDENTAL surface extension on `crates/lumen` if FLAG 3 lands as recommended, 1 integration point (HTTP route -> existing `query_with` seam), 1 day end-to-end, single user outcome. No split needed. |
| 7 | Technical notes identify constraints | PASS | `user-stories.md` § Technical Notes names: the existing `query_with` seam (`crates/lumen/src/store.rs:89`), the existing `Predicate::matches` conjunctive composition (`crates/lumen/src/predicate.rs:53-66`), the predicate-extension flag (FLAG 3) with two shapes (lumen surface extension vs handler-side filtering), the parsing location (a `parse_body_contains` free function in `crates/log-query-api/src/lib.rs` alongside `parse_min_severity`), the order-of-checks (tenant -> window parse -> window cap -> `min_severity` parse -> `body_contains` parse -> dispatch -> result cap -> serialise), the composition-with-`min_severity` semantics (conjunctive AND via `Predicate::matches`), and seven named mutation targets. The System Constraints section names every `query-http-common` surface the slice consumes (cap constants, reason consts, envelope helper, tenant seam, bounds parser) and forbids re-implementation. |
| 8 | Dependencies resolved or tracked | PASS | All dependencies are present in the repository on the slice-prior tag: ADR-0047 (log-query-api contract), ADR-0050 (caps), ADR-0052 (`min_severity` parameter, composition partner), ADR-0054 (`query-http-common` extraction, the M-5 source), `lumen::LogStore::query_with` (`crates/lumen/src/store.rs:89`), `lumen::Predicate::matches` (`crates/lumen/src/predicate.rs:53-66`), the full `query-http-common` public surface (`MAX_RESULT_ROWS`, `MAX_WINDOW_SECONDS`, four `REASON_*` consts, `error_response`, `resolve_tenant_or_refuse`, `parse_time_range`). The six flags to DESIGN are tracked in `wave-decisions.md`, NOT blockers. |
| 9 | Outcome KPIs defined with measurable targets | PASS | `outcome-kpis.md` carries four KPIs, each with Who / Does what / By how much / Baseline / Measured by / Type. KPI-1 has a 100% per-record honesty target on a named fixture; KPI-2 is a 100% green gate on three existing suites; KPI-3 carries explicit static-grep + line-count CI assertions on `crates/log-query-api/src/lib.rs` (under 30 new LOC, zero new duplications of any `query-http-common` surface); KPI-4 names the case-sensitivity acceptance-test function. Hypothesis section at the bottom restates the four KPIs as a single falsifiable claim. |

## DoR Status: PASSED

Ready for DESIGN handoff. The six flags in `wave-decisions.md` § "Flags to
DESIGN" are tracked design choices, NOT DoR gaps; DESIGN pins each one by
reading the recommendations, weighing the reason, and recording the
choice in its own `design/wave-decisions.md` or in the ADR-0055 it
(likely) authors per FLAG 6.

## Anti-pattern detection sweep

The LeanUX template names six anti-patterns; the slice's artefacts have
been swept for each.

| Anti-pattern | Signal | Found? | Evidence |
|---|---|---|---|
| Implement-X | "Implement auth", "Add feature" | NO | The slice's narrative starts from user pain (Sara holds an error string, pipes through `jq`, costs seconds). The story titles are "A known substring narrows the response to matching records", "Unknown substring returns the calm empty array", etc.; none start with "Implement" or "Add". |
| Generic data | `user123`, `test@test.com` | NO | Every example uses real tenant ids (`acme-prod`, `globex-staging`), real persona names (Sara Mendez, Marcus Webb, Priya Raman), real timestamps (epoch seconds 1716200005..1716200030, nanoseconds 1_716_200_005_000_000_000..), and real domain strings (`kafka timeout connecting to broker-3`, `kafka timeout after 30s on topic orders`, `checkout: payment timeout`, `checkout: heartbeat`). |
| Technical AC | "Use JWT tokens" | NO | Every AC is phrased as an observable outcome: "only records whose body contains the substring appear", "the response is the calm empty bare array `[]`", "the body NEVER contains the substring `broker-3`". The lone exception (the `query-http-common` sole-provider AC) is named explicitly as a code-shape constraint, not as a behavioural promise; it traces to KPI-3 as a validation of M-5, not as a user-facing requirement. |
| Technical scenario title | "FileWatcher triggers refresh", "Observer writes state.json" | NO | All six scenario titles describe user-observable outcomes: "A known substring narrows the response", "An unmatched substring returns the calm empty array, never 404", "Parameter absent returns every record in the window", "An empty body_contains value is a redacted 400", "The match is case-sensitive", "Cross-tenant isolation — tenant B never sees tenant A's matches". |
| Oversized story | >7 scenarios, >3 days | NO | 6 scenarios, 1 day. `story-map.md` § Scope Assessment passes the carpaccio gate. |
| Abstract requirements | No concrete examples | NO | Each story has a concrete Elevator Pitch with Before/After/Decision enabled. Each scenario has a concrete data table or a concrete pre-condition. The Domain Examples section under US-01 carries six concrete narratives. |

No anti-patterns detected. The slice is ready for DESIGN.
