# Definition of Ready — Validation

Feature: `cli-list-items-subcommand-v0`
Reviewer: Luna (self-review at DISCUSS close; second pass
scheduled at handoff to DESIGN per `nw-po-review-dimensions`
skill).
Date: 2026-05-19.

## Per-story DoR (9-item hard gate)

### US-01: Operator lists every item Cinder currently has in a given tier for a given tenant

| # | DoR Item | Status | Evidence |
|---|----------|--------|----------|
| 1 | Problem statement clear, domain language | PASS | "After `stats` shows `cold=47`, the next natural operator question is 'which items are those 47 cold ones?' Today that question is unanswerable from the CLI. The `stats` subcommand surfaces COUNTS via `list_by_tier(tenant, tier).len()` ... but it does NOT surface the item ids themselves. To answer 'which items?', Priya has to write a Rust harness ..." Uses tier, item id, Cinder, TieringStore, list_by_tier, hot/warm/cold, place, migrate, tenant manifest, scripted pipelines, `xargs`, lex sort, `HashMap` iteration order — all domain terms grounded in the existing Cinder/CLI implementation. |
| 2 | User/persona identified | PASS | Priya the platform operator, multi-tenant Kaleidoscope deployment for a fintech, already uses `kaleidoscope-cli ingest`, `kaleidoscope-cli read`, `kaleidoscope-cli stats`, `kaleidoscope-cli migrate` daily (inherited from the seven predecessor features). Uses standard Unix text tools (`grep`, `wc -l`, `xargs`, `sort`, `comm`, `diff`) on stdout output. Expects fail-fast behaviour (non-zero exit + descriptive stderr) on invalid tier names. Expects determinism (byte-identical stdout across runs). Does NOT expect the subcommand to touch the Lumen store or mutate any Cinder state. |
| 3 | 3+ domain examples with real data | PASS | Five examples: happy path with three cold items `acme/batch-00099`, `acme/batch-00007`, `acme/batch-00041` placed in non-lex insertion order, decoy Hot item `acme/batch-00050`, the literal stdout bytes shown lex-sorted; edge case (empty result) with `acme` having zero warm items showing empty stdout + exit 0; edge case (single item) with `globex/batch-00000` showing the trivial sort case; tenant-isolation case with `acme` and `globex` both holding `shared/batch-00042` showing only `acme`'s entry surfaces; error case (invalid tier) with `COLD` showing the empty-stdout + non-empty-stderr + non-zero-exit + no-store-open contract. Real values throughout: `acme`, `globex`, `acme/batch-00099`, `acme/batch-00007`, `acme/batch-00041`, `acme/batch-00050`, `acme/batch-00000`, `acme/batch-00042`, `shared/batch-00042`, `globex/batch-00000`, `/tmp/data`, `/tmp/kdata`, real tier names (`hot`, `warm`, `cold`), real source-file references with line numbers. |
| 4 | UAT scenarios (3-7 in Given/When/Then) | PASS | 5 scenarios: happy-path cold enumeration with lex-sort (OK1 correctness with sort sub-property), empty-tier result (OK1 N=0 case), invalid-tier fail-fast (OK3), tenant-isolation (OK2), determinism across two successive invocations (OK1 determinism sub-property). |
| 5 | AC derived from UAT | PASS | 10 AC bullets, each maps to an observable byte-level / state-level invariant from a scenario (exact lex-sorted stdout bytes for happy-path; empty stdout for empty result; stderr substring on invalid tier; tenant-isolation `list_by_tier(globex, ...)` unchanged; byte-identical stdout across two runs; Cinder store byte-equivalent before/after every invocation; Lumen store byte-equivalent before/after every invocation; new acceptance test file added; locked test files continue to pass green unmodified; `print_usage` update optional; no new external dependency). |
| 6 | Right-sized | PASS | 1 story, 1 function-level addition in `lib.rs` (new `list_items(...)` library function — STRICTLY THINNER than the predecessor `migrate` because no `get_entry` pre-flight, no `from`/`to` resolution, no mutation, no `SystemTime::now()` argument, no new `Error` variant), 1 dispatch wiring change in `main.rs` (new `Some("list-items")` arm + new `run_list_items(...)` helper + `print_usage` update), 1 new test file with 5 scenarios, 1 manifest entry. Estimated effort: well under 1 day. Strictly thinner than the immediate predecessor (`cli-migrate-subcommand-v0`); structurally the same shape but each piece is shorter. |
| 7 | Technical notes identify constraints | PASS | File paths, line numbers, manifest entry, dependency posture (no new external dependency — `cinder::TieringStore::list_by_tier`, `FileBackedTieringStore::open`, `cinder::CinderRecorder`, `Tier` enum, `ItemId`, `parse_tier` helper, `cinder_base()` helper, `parse_positional` helper, `aegis::TenantId` all already wired; `Error::InvalidTier`, `Error::CinderOpen`, `Error::Io` all already exist). NO new `Error` variant. The explicit non-prescription of internal implementation details beyond the choice of one new free function shape (DESIGN-locked per `wave-decisions.md` D-FunctionShape), the choice of stderr summary line (D-StderrSummary), the choice of `parse_tier` visibility (DESIGN's call). The hard rule "DO NOT modify any locked test file" is restated as D-LockedTests. |
| 8 | Dependencies tracked | PASS | All Cinder-side APIs exist (`crates/cinder/src/store.rs:102` for `list_by_tier`; the file-backed adapter implements the same trait); `cinder::FileBackedTieringStore` is already used by `stats_with_tiers` (`crates/kaleidoscope-cli/src/lib.rs:377-378`) and by `migrate` (`lib.rs:445-446`); `cinder::CinderRecorder` already used by `stats_with_tiers` (`lib.rs:377`) and `migrate`'s no-flag arm (`lib.rs:443`); `cinder::Tier` and `cinder::ItemId` already imported and used; `parse_tier` helper at `lib.rs:475-482`; `cinder_base(data_dir)` helper at `lib.rs:130-132`; `parse_positional` helper at `crates/kaleidoscope-cli/src/main.rs:296-302`; `aegis::TenantId` already a dependency; `Error::InvalidTier`/`CinderOpen`/`Io` variants all already exist (`lib.rs:79-81, :77, :82`). No unresolved internal or external dependencies. |
| 9 | Outcome KPIs defined with measurable targets | PASS | Maps to OK1-CLI-list-items-correctness (principal: 100% of valid invocations produce stdout whose lines are EXACTLY the set returned by `list_by_tier(tenant, tier)`, sorted lex; with three sub-properties: lex-sort masking `HashMap` randomisation, byte-identical stdout across two successive runs, empty stdout for empty result), OK2-CLI-list-items-tenant-isolation (100% of invocations against tenant T surface only entries with key `(T, *)`; 0% surface entries from any other tenant; cross-tenant Cinder state byte-equivalent before/after), OK3-CLI-list-items-invalid-tier-fail-fast (100% of non-lower-case-hot/warm/cold invocations produce non-zero exit + stderr containing the invalid value + Cinder store unopened). All three targets are quantitative and falsifiable via the named test file. |

### US-01 DoR Status: PASSED

## Feature-level DoR

| # | Item | Status | Evidence |
|---|------|--------|----------|
| F1 | Journey artefact (visual) | NOT PRODUCED — accepted | Per `wave-decisions.md`, the journey is so thin (one operator action: invoke `list-items <tenant> <data_dir> <tier>` and read stdout) that a separate journey visual would duplicate the Elevator Pitch in US-01. The seven reference features in this cluster collectively cover the underlying CLI operator mental model. Acknowledged gap; will not be remediated. |
| F2 | Journey artefact (YAML schema) | NOT PRODUCED — accepted | Same rationale as F1. The journey schema in `nw-design-methodology` is for multi-step user journeys; this feature has one step. |
| F3 | Journey artefact (Gherkin `.feature`) | INTENTIONALLY OMITTED | Per the project convention (inherited from the seven reference features and noted in the task brief): this project's acceptance idiom is Rust `#[test]` functions with `// Given / // When / // Then` comment blocks, not Gherkin `.feature` files. The Given/When/Then text lives inside `user-stories.md` (UAT Scenarios sections). DISTILL writes the Rust tests. |
| F4 | Shared artefact registry | NOT PRODUCED — accepted | The cross-feature shared artefacts (`tenant_id` positional argument, `data_dir` positional argument, `cinder_base(data_dir)` helper, `TieringStore::list_by_tier` API, `CinderRecorder` quiescent-recorder pattern, the lower-case tier convention, the `parse_tier` helper, the `Error::InvalidTier` variant) are tracked in the seven reference features and the source code itself. The output-shape contracts (one bare item id per line on stdout, lex-sorted, terminated by `\n`; empty stdout for empty result; non-zero exit + stderr line for invalid tier) are restated as System Constraints in `user-stories.md`. |
| F5 | Story map | PASS | `discuss/story-map.md` with one-activity backbone, walking-skeleton justification (N/A — the predecessor cluster's substrate already exists and the Cinder `TieringStore::list_by_tier` API already exists and is already in production use), single-slice rationale, cross-feature alignment, scope assessment, priority rationale section, all three KPI guardrails articulated. |
| F6 | Prioritization | INTEGRATED into `story-map.md` | The single-slice structure renders a separate `prioritization.md` redundant. The `## Priority Rationale` section in `story-map.md` covers the ordering logic and partial-ship reasoning. |
| F7 | Outcome KPIs | PASS | `discuss/outcome-kpis.md` with OK1 (principal — list-items correctness with three sub-properties: lex-sort, determinism, empty-result), OK2 (tenant-isolation guardrail inherited from `list_by_tier`'s per-tenant filter), OK3 (invalid-tier fail-fast), each with quantitative target, baseline, and measurement plan. |
| F8 | User stories | PASS | `discuss/user-stories.md` with US-01, including complete Elevator Pitch with the real entry-point shell command (`kaleidoscope-cli list-items acme /tmp/data cold`) and the concrete byte-level output (three lex-sorted stdout lines shown in the "After" line for the three-item case; empty stdout for the zero-item case; companion error-case stderr line behaviour described concretely). |
| F9 | Per-slice files | PASS | 1 file under `discuss/slices/` (`slice-01-list-items-subcommand-enumerates-tier.md`, within the 100-line spirit of the cap when considering the body content; some additional clarifying sections expand it but the core slice brief — Goal, IN scope, OUT scope, Rejected alternatives, Learning hypothesis, AC, Dependencies, Reference class, Effort, DoD — fits the structural cap). |
| F10 | Wave decisions | PASS | `discuss/wave-decisions.md` with D1-D4 (pre-wave decisions), D-LowerCase (explicit task-brief decision), D-ReadOnly (Cinder side never mutated), D-NoLumenTouch (Lumen never opened), D-Sort (lex-sort at the CLI boundary masks HashMap randomisation), D-OutputShape (one bare id per line, no key=value), D-FunctionShape (DESIGN-locked signature), D-StderrSummary (DESIGN-locked summary line presence), D-ErrorVariant (NO new variants — all reused), D-StderrWording (DESIGN-locked exact wording), D-NewTestFile (new file location), D-LockedTests (do-not-modify hard rule), D-OutOfScope-Json / D-OutOfScope-Observe / D-OutOfScope-CrossTenant / D-OutOfScope-Historical / D-OutOfScope-Pagination (all five task-brief out-of-scope items), D-NoSSOT. |
| F11 | Right-sized (Elephant Carpaccio) | PASS | 1 story, 1 bounded context (`kaleidoscope-cli` crate), 2 modified files in `src/`, 1 new test file, 1 manifest line-level change. Estimated effort: well under 1 day. STRICTLY THINNER than the predecessor `cli-migrate-subcommand-v0` (no mutation, no `get_entry` pre-flight, no new `Error` variant). `story-map.md` "Scope Assessment: PASS". |
| F12 | SSOT impact assessed | PASS | `wave-decisions.md` D-NoSSOT: no SSOT modification. Same posture as the seven reference features. |
| F13 | Cross-feature contract honoured | PASS | `user-stories.md` System Constraints pin the output-shape contract (one bare item id per line on stdout, lex-sorted, terminated by `\n`; empty stdout for empty result; non-zero exit + stderr line for invalid tier), the positional-argument convention (extends the existing two-positional shape with one new positional argument `<tier>` — strictly fewer arguments than `migrate`'s four), the stream contract (stdout for success records, stderr for failure — inherited from the binary's existing main.rs error printer at `crates/kaleidoscope-cli/src/main.rs:70-73`), the lower-case tier convention (mirrors the `migrate` subcommand's parse-side enforcement at `lib.rs:432-434, :475-482` and the rendering side at `lib.rs:489-495`), the no-Lumen-touch invariant (inherited from `migrate`'s D-NoLumenTouch), the tenant-isolation invariant (inherited from `cinder::TieringStore`'s per-tenant key filter at `crates/cinder/src/store.rs:194-196`), and the read-only Cinder posture (NEW — D-ReadOnly). The locked test files for the seven prior features continue to pass green UNMODIFIED. |

## Anti-pattern scan

| Anti-pattern | Detected? | Notes |
|--------------|-----------|-------|
| Implement-X | No | Story framed as "Priya runs `kaleidoscope-cli list-items acme /tmp/data cold` and sees N lex-sorted item-id lines on stdout in milliseconds, exit 0, without writing Rust" — outcome-first from the operator perspective. The three operator decisions enabled (manual rebalancing follow-up / sanity check / scripted pipeline) are named explicitly with concrete dogfood examples (`... | xargs -I {} migrate ... {} warm`). |
| Generic data | No | Real names: Priya, `acme`, `globex`. Real item ids: `acme/batch-00099`, `acme/batch-00007`, `acme/batch-00041`, `acme/batch-00050`, `acme/batch-00000`, `acme/batch-00042`, `shared/batch-00042`, `globex/batch-00000`. Real paths: `/tmp/data`, `/tmp/kdata`. Real tier names: `hot`, `warm`, `cold` (and the explicit anti-examples `COLD`, `lukewarm`). Real source-file references: `crates/kaleidoscope-cli/src/lib.rs:77, :79-81, :82, :98-100, :130-132, :377-378, :383, :432-446, :443, :445-446, :475-482, :489-495`; `crates/cinder/src/store.rs:71-72, :102, :119, :190-198, :194-196`; `crates/cinder/src/metrics.rs`; `crates/kaleidoscope-cli/src/main.rs:52-66, :70-73, :214-240, :261, :264-294, :296-302`. |
| Technical AC | No blocking instances | AC pin the wire-observable invariants (exact lex-sorted stdout byte sequence on success; empty stdout for empty result; empty stdout + non-empty stderr + non-zero exit on invalid-tier failure; cross-tenant Cinder state byte-equivalent for OK2; byte-identical stdout across two runs for determinism; Cinder store byte-equivalent before/after every invocation; Lumen store byte-equivalent before/after every invocation). They reference the structural mirror of the predecessor's recorder construction and `run_migrate` helper shape as implementation patterns but do NOT prescribe novel internal mechanisms; the structural-mirror references are facts about the precedent, not novel design constraints. |
| Technical scenario titles | No | Scenarios titled by user outcome: "Operator lists every cold item for `acme` — sees N lines on stdout, sorted lex", "Operator lists items for an empty tier — sees empty stdout, exit 0", "Operator types an invalid tier value — fail-fast with stderr naming the invalid value", "Tenant isolation — listing `acme`'s cold items does not surface `globex`'s same-named cold items", "Determinism — the same invocation produces byte-identical stdout across two runs". Not "TieringStore::list_by_tier is called with arguments" or "FileBackedTieringStore::open recorder slot accepts boxed dyn trait". |
| Oversized story | No | 1 story, 5 UAT scenarios, 10 AC, well under 1 day effort. Strictly thinner than the predecessor. |
| Abstract requirements | No | Every AC has a specific value (exact byte sequence for happy path stdout; substring assertions for stderr content; empty bytes for empty result), a numeric exit code (0 for success, non-zero for failure), or a state-level invariant (pre-call vs post-call `list_by_tier().len()` equality; cross-tenant `list_by_tier(globex, ...)` set equality; Lumen directory byte-equivalence). |

## Reviewer self-pass (Dimension 0 — Elevator Pitch, BLOCKING)

| Story | Section present | Real entry point | Concrete output | Job connection | Verdict |
|-------|-----------------|------------------|-----------------|----------------|---------|
| US-01 | yes (Before / After / Decision enabled triple) | yes — `kaleidoscope-cli list-items acme /tmp/data cold` (the actual shell command an operator types — NEW subcommand on the existing binary) | yes — three literal stdout lines (`acme/batch-00007`, `acme/batch-00041`, `acme/batch-00099`) shown lex-sorted in the "After" line; empty-stdout case shown for the N=0 branch; companion error-case stderr line behaviour described concretely (`COLD` named verbatim, `lukewarm` named verbatim) | yes — "Priya can decide and execute three operationally distinct workflows from one CLI invocation: 1. 'Show me every cold item for `acme` so I can identify which ones to manually migrate back to warm' (manual rebalancing follow-up to `stats`). 2. 'List every warm item for `acme` and diff it against the tenant manifest I have in my notes' (sanity check). 3. 'Pipe every cold item through `migrate` to walk them back to warm in one shell loop' (scripted pipeline: `kaleidoscope-cli list-items acme /tmp/data cold \| xargs -I {} kaleidoscope-cli migrate acme /tmp/data {} warm`)." | PASS |

Slice-level check (Dimension 0 item 5): the single slice (Slice
01) is tagged operator-visible (NOT `@infrastructure`). The
story's Elevator Pitch names a real user-invocable CLI command
and the byte-level stdout output the operator's shell session
will display. There is no slice-level infrastructure-only
blocking concern. PASS at slice level.

## Confirmation-bias detection (Dimensions 1-4 self-check)

| Dimension | Check | Verdict |
|-----------|-------|---------|
| 1.1 Technology bias | Story specifies the existing CLI binary, the existing `TieringStore` trait API (specifically the `list_by_tier(tenant, tier) -> Vec<ItemId>` method already in production use at `crates/kaleidoscope-cli/src/lib.rs:383`), and a plain-text one-bare-id-per-line stdout shape. No specific JSON parser prescribed; no specific datetime library prescribed (no timestamps at all); the new tier-argument parser reuses the existing `parse_tier` helper. The output is consumable by standard Unix text tools that any operator already has. | PASS |
| 1.2 Happy path bias | Story includes two non-happy-path scenarios out of five: empty-tier result (OK1 N=0 case — a degenerate success branch that requires explicit handling to avoid emitting a placeholder line) and invalid-tier fail-fast (OK3). The tenant-isolation scenario (OK2) is also a non-trivial guardrail. The determinism scenario explicitly tests the property that the HashMap iteration randomisation would violate without the boundary sort — this is sad-path-shaped (it would surface as a flaky test on the second run if the sort were missing). | PASS |
| 1.3 Availability bias | The chosen output shape (one bare item id per line, lex-sorted) is justified by the operator's existing tooling (Unix text utilities, `xargs -I {} migrate ... {} ...`) and by the cross-feature pattern (`stats` and `migrate` use `key=value` for single-event reports; `list-items` is one-record-per-line because the output is N records). The lex-sort decision (D-Sort) is justified by reference to `cinder::InMemoryTieringStore::list_by_tier` at `crates/cinder/src/store.rs:190-198` which iterates a HashMap in randomised order. The lower-case tier convention (D-LowerCase) is justified by reference to the existing `migrate` parse-side enforcement at `lib.rs:432-434, :475-482` and the rendering side at `lib.rs:489-495`. | PASS |
| 2.1 Missing stakeholder perspectives | Primary: platform operator (Priya). Secondary: Cinder maintainer + Lumen maintainer (covered by the meta-AC — the existing locked test files for the seven predecessor features continue to pass green UNMODIFIED, so the extension does not perturb any shipped feature). Tertiary: scripting/automation consumers (covered by the explicit operator-friendly stdout/stderr contracts: N lines on success with one bare id per line, one stderr line containing the offending value on failure, non-zero exit). The cross-feature contract is honoured: the new subcommand inherits the positional-argument convention, the lower-case tier convention, and the no-Lumen-touch invariant from the predecessor cluster. | PASS |
| 2.2 Missing error scenarios | One distinct error scenario specified: invalid-tier (OK3 — parse-side error before the Cinder store is opened). Plus the no-Lumen-touch invariant covering every call branch (success, empty result, invalid tier). Plus the tenant-isolation guardrail (OK2). Plus the determinism property (which is also an implicit error guardrail — the test would flag a missing sort step). Cinder open errors are surfaced via the existing `Error::CinderOpen(MigrateError)` variant and bubble to the binary's existing error printer in `main()`. NO new error variant introduced. | PASS |
| 2.3 Missing NFRs | NFRs covered: list-items correctness (OK1 — operator-facing correctness invariant on the stdout bytes), determinism (OK1 sub-property — byte-identical stdout across two runs, masking the HashMap randomisation), tenant isolation (OK2 — operator-facing safety guarantee), fail-fast on invalid tier (OK3 — operator-facing fail-fast guarantee), read-only invariant on the Cinder store (Cinder WAL+snapshot byte-equivalent before and after every invocation), no-Lumen-touch invariant (Lumen WAL+snapshot byte-equivalent before and after every call). Performance: the v0 contract is "one `list_by_tier` call plus one `vec.sort()` plus N `writeln!` calls per invocation" which is whatever the existing trait method returns plus an `O(N log N)` sort over a small N; acceptable at v0 tier sizes. | PASS |
| 3.1 Vague performance requirements | All "100%"/"0%" claims are quantitative and falsifiable via the named test. No "fast", "scalable", "performant" adjectives without a number. The "in milliseconds" claim in the Elevator Pitch is descriptive operator language inherited from the seven predecessor features, not a normative requirement; the AC do not quantify a latency target. | PASS |
| 3.2 Ambiguous requirements | Every AC pins exact byte sequences (the three-line lex-sorted output for the happy path, empty bytes for the empty result), exit code values (0 for success, non-zero for failure), substring assertions on stderr (the verbatim invalid value for OK3), or state-level equality (`list_by_tier(globex, ...)` set unchanged for OK2; `list_by_tier(acme, Tier::Hot).len()` unchanged across the invalid-tier branch). | PASS |
| 4 Testability | Every AC is a property of (a) the captured stdout bytes after the list-items function returns, (b) the captured stderr bytes (or the `Error`'s `Display` impl, depending on D-StderrSummary's resolution by DESIGN), (c) the returned `Result`, (d) a subsequent `cinder.list_by_tier(tenant, tier)` call against a freshly-opened Cinder store, or (e) the presence/byte-content of the `lumen_base(data_dir)` directory. Every AC is automatable via `String::from_utf8` + `Vec<u8>` byte comparison + `str::contains` substring matching + `assert_eq!` integer / vec comparison + filesystem checksum or existence assertion. The determinism property is doubly testable: directly via the new test file's two-invocation sub-scenario AND indirectly via the lex-sort step in the library function being explicit and reviewable in the source. | PASS |
| 5.1 Largest bottleneck | The largest gap in the operator's daily Cinder workflow is "after `stats` shows me `cold=47`, I want to know WHICH 47 items, but the only way today is to write a Rust harness against `cinder::FileBackedTieringStore::list_by_tier` or grovel through the Cinder snapshot file format". This feature addresses exactly that gap by adding the missing CLI surface that exposes the same read `stats` already consumes for its count line. | PASS |
| 5.2 Simpler alternatives considered | Considered and rejected: (a) JSON output (`--json` flag) — rejected in `wave-decisions.md` D-OutOfScope-Json, plain text is already machine-parseable by every Unix text tool; (b) cross-tenant aggregate (`list-items <data_dir> <tier>` without `<tenant>`) — rejected in D-OutOfScope-CrossTenant, the per-tenant API is the v0 primitive and a shell loop covers the cross-tenant case; (c) time-bound historical state (`--at <timestamp>`) — rejected in D-OutOfScope-Historical and deferred to ADR-0039 §7; (d) pagination (`--limit`, `--offset`) — rejected in D-OutOfScope-Pagination, v0 cardinality is small; (e) `--observe-otlp` — rejected in D-OutOfScope-Observe, `list_by_tier` is a pure read with no `record_list` method in Cinder's `MetricsRecorder` trait at `crates/cinder/src/metrics.rs`; (f) `key=value` output shape (e.g. `item=acme/batch-00007`) — rejected in D-OutputShape, the one-bare-id-per-line shape is the natural representation for an N-record list and pipes directly into `xargs -I {} ...`. | PASS |
| 5.3 Constraint prioritization | The dominant constraint is correctness (OK1 — the stdout lines must be exactly the set returned by `list_by_tier`, sorted lex, with byte-identical stdout across two runs). The two guardrails (OK2 tenant isolation, OK3 invalid-tier fail-fast) are correctly weighted as the safety and input-validation guarantees the operator depends on. There is no cross-process or concurrency invariant to prioritize because the extension has only one read per invocation and runs in a single thread. The lex-sort step at the boundary is the EXPLICIT determinism guarantee (D-Sort) — without it, OK1's byte-identical-across-runs sub-property would fail. | PASS |
| 5.4 Data-justified | This is not a performance optimization. The decision to use one `list_by_tier` call plus one `vec.sort()` plus N `writeln!`s per invocation is justified by the underlying API shape (`Vec<ItemId>` per `crates/cinder/src/store.rs:102`) and by the operator's natural consumer (Unix text tools that expect one record per line). The decision to lex-sort at the CLI boundary (vs at the trait/store layer) is justified by the documented HashMap randomisation in `cinder::InMemoryTieringStore::list_by_tier` and by the desire to keep the trait API shape minimal (Cinder MAY at some point change the underlying data structure to a `BTreeMap` which would return items in sorted order; the CLI boundary sort is robust to that change either way). The decision to omit `--observe-otlp` (D-OutOfScope-Observe) is justified by the absence of a `record_list` method in Cinder's `MetricsRecorder` trait. | PASS |

## DoR Status: PASSED — with four honestly-recorded artefact gaps

The story-level DoR (9 items × 1 story = 9 checks) is PASSED
with evidence on every item. The feature-level DoR is PASSED
with the following honestly-recorded artefact gaps (inherited
posture from the seven predecessor features):

- **F1, F2 (journey artefacts)**: deliberately not produced.
  The journey is one step (operator runs `list-items <tenant>
  <data_dir> <tier>` and reads stdout); a multi-step journey
  visual or YAML would duplicate the Elevator Pitch in US-01.
  The seven reference features' journey visuals collectively
  cover the underlying CLI operator mental model. Acknowledged;
  will not be remediated.
- **F3 (Gherkin `.feature` file)**: deliberately not produced.
  Per the project convention, this project's acceptance idiom
  is Rust `#[test]` functions with `// Given / // When /
  // Then` comment blocks. The Given/When/Then specification
  lives inline in `user-stories.md`.
- **F4 (shared artefact registry)**: not produced. The
  cross-feature shared artefacts are tracked in the seven
  reference features and the source code itself; the output-
  shape contracts are restated as System Constraints in
  `user-stories.md`. Producing a new registry that copies the
  reference features' rows would add no value.
- **F6 (separate `prioritization.md`)**: not produced;
  integrated into `story-map.md`'s `## Priority Rationale`
  section per the `nw-leanux-methodology` skill template,
  because a one-slice feature does not earn a separate
  prioritization document.

Anti-pattern scan clean. Dimension 0 elevator-pitch check
PASSES with the real entry point (`kaleidoscope-cli list-items
acme /tmp/data cold` — a NEW subcommand on the existing
binary) and the concrete byte-level output (three lex-sorted
stdout lines on success; empty stdout for the N=0 case;
non-empty stderr line with the offending value on the
invalid-tier failure path). Dimensions 1-5 confirmation-bias
self-check PASSES.

The DISCUSS wave is ready to hand off to `nw-solution-architect`
for DESIGN.
