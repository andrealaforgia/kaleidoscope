# Definition of Ready — Validation

Feature: `cli-stats-time-range-v0`
Reviewer: Luna (self-review at DISCUSS close; second pass scheduled
at handoff to DESIGN per `nw-po-review-dimensions` skill).
Date: 2026-05-19.

## Per-story DoR (9-item hard gate)

### US-01: Operator counts a tenant's records within an ISO 8601 time window

| # | DoR Item | Status | Evidence |
|---|----------|--------|----------|
| 1 | Problem statement clear, domain language | PASS | "Today the `stats` subcommand cannot answer either question directly. The dispatcher in `main.rs` calls `kaleidoscope_cli::stats_with_tiers(...)` (`crates/kaleidoscope-cli/src/main.rs:226-235`), which always calls `lumen.query(tenant, TimeRange::all())` at `crates/kaleidoscope-cli/src/lib.rs:359-361`." Uses Lumen, Cinder, TimeRange, NDJSON, `observed_time_unix_nano`, `jq`, ISO 8601 UTC, half-open interval, tenant, state-snapshot, tier — all domain terms grounded in the existing CLI implementation, the lumen crate's `TimeRange` doc-comment, the cinder crate's `TieringStore` trait, and the prior five `kaleidoscope-cli` features' shipped behaviour. |
| 2 | User/persona identified | PASS | Priya the platform operator, multi-tenant Kaleidoscope deployment for a fintech, already uses `kaleidoscope-cli stats <tenant> <data_dir>` for full-tenant summary counts and `kaleidoscope-cli read <tenant> <data_dir> --since X --until Y` for windowed record dumps. Now needs the same windowing on `stats` to count and bracket the window without `jq` aggregation. Same persona as the five reference features. |
| 3 | 3+ domain examples with real data | PASS | Four examples: (1) Yesterday's window for `acme` with both flags set — `--since 2026-05-18T00:00:00Z --until 2026-05-19T00:00:00Z` / parsed `since_ns = 1_779_062_400_000_000_000`, `until_ns = 1_779_148_800_000_000_000` / 7 records evenly spaced 4 hours apart / 6 in window / stdout shape with the three Lumen lines and the unchanged Cinder snapshot. (2) Records since 15:30 with only `--since` set — `--since 2026-05-19T15:30:00Z` / `since_ns = 1_779_205_800_000_000_000` plus the symmetric `--until`-only example. (3) Empty window — `--since 2026-05-17T02:00:00Z --until 2026-05-17T03:00:00Z` returns zero records / stdout is exactly `records=0\n` followed by the unchanged Cinder snapshot (D-EmptyWindow). (4) Typo on `--since` mid-incident — `--since yesterday` / stderr message naming `--since` and the verbatim bad value, exit code 1 (D-NoNewError). Real values throughout: `acme`, `/tmp/data`, real ISO 8601 timestamps (`2026-05-18T00:00:00Z`, `2026-05-19T15:30:00Z`, `2026-05-17T02:00:00Z`), real nanosecond literals computed for those timestamps, real CLI argv lists. |
| 4 | UAT scenarios (3-7 in Given/When/Then) | PASS | 7 scenarios: bounded window count + earliest/latest (OK1 + OK2), no-flag byte equivalence (OK4), `--since`-only (OK1 half-bounded), `--until`-only (OK1 half-bounded), empty window (D-EmptyWindow), Cinder lines time-independent (OK3 / D-CinderScope), invalid `--since` (D-NoNewError). |
| 5 | AC derived from UAT | PASS | 14 AC bullets, each maps to an observable invariant from a scenario (TimeRange-driving control on `stats_with_tiers`, half-open `[since, until)` contract on the records count, windowed earliest/latest derivation, no-flag byte equivalence, D-EmptyWindow empty-window contract, D-CinderScope Cinder lines unchanged, two-flag parsing via the existing `parse_flag_iso` helper in `main.rs`, half-bounded defaults, ISO 8601 accepted shape via the existing parser, fail-fast contract for `--since`, fail-fast contract for `--until`, `print_usage` doc update including D-CinderScope note, locked-test non-regression meta-AC, new test file existence). |
| 6 | Right-sized | PASS | 1 story, 1 TimeRange-driving control change on `stats_with_tiers`, 1 new `parse_time_range(args)?` call in `run_stats_with`, 1 new test file with 6 test functions, <= 1 day effort. STRICTLY THINNER than the predecessor `cli-read-time-range-v0` because the parser-side work is already done in the prior wave. |
| 7 | Technical notes identify constraints | PASS | File paths, line numbers, manifest entry, dependency posture (no new external crate; parser reused unchanged from `cli-read-time-range-v0` per D-NoNewError), explicit non-prescription of internal implementation details (DESIGN-owned choice for the exact signature shape on `stats_with_tiers`), explicit half-open `[start, end)` semantics inherited from `lumen::TimeRange`, explicit no-modification list for the legacy `stats()` function and the locked test files and the lumen `TimeRange` type and the existing parser, explicit D-CinderScope (the Cinder loop at lines 375-380 is NOT touched), explicit D-EmptyWindow contract (no `earliest=` / `latest=` lines on empty window). |
| 8 | Dependencies tracked | PASS | `lumen::TimeRange` already exists with the correct semantics (`crates/lumen/src/record.rs:97-120`); no change to the `lumen` crate required. `kaleidoscope_cli::parse_iso8601_utc_nanos` already exists from `cli-read-time-range-v0` (`crates/kaleidoscope-cli/src/lib.rs:528-647`); no new parser code required. `kaleidoscope_cli::stats_with_tiers` already opens Lumen and Cinder and calls `query` with a `TimeRange` at `crates/kaleidoscope-cli/src/lib.rs:357-361`; change is at the construction site. `parse_time_range(args)` / `parse_flag_iso(args, flag)` helpers already exist at `crates/kaleidoscope-cli/src/main.rs:188-214` and are subcommand-neutral. `aegis::TenantId`, `cinder::*`, `lumen::*`, `serde_json` — all existing dependencies. No unresolved external dependencies. |
| 9 | Outcome KPIs defined with measurable targets | PASS | Maps to OK1-bounded-window-records (principal, 100% equality of the `records=N` line vs `lumen.query(tenant, TimeRange::new(since_ns, until_ns)).len()`), OK2-bounded-window-earliest-latest (`earliest=` / `latest=` lines equal `format_iso8601_utc_nanos` of the windowed min/max, omitted entirely on empty window per D-EmptyWindow), OK3-cinder-lines-unchanged (100% byte-identity of the Cinder lines across different `TimeRange` invocations — pins D-CinderScope), OK4-no-flag-byte-equivalence (100% byte equivalence of stdout when neither flag set, locked tests pass green). All four targets are quantitative and falsifiable via the named test file. |

### US-01 DoR Status: PASSED

## Feature-level DoR

| # | Item | Status | Evidence |
|---|------|--------|----------|
| F1 | Journey artefact (visual) | NOT PRODUCED — accepted | Per `wave-decisions.md`, the journey is so thin (one operator action: invoke `stats` with two new optional flags) that a separate journey visual would duplicate the Elevator Pitch in US-01. The five reference features in this cluster collectively cover the underlying mental model. Acknowledged gap; will not be remediated. |
| F2 | Journey artefact (YAML schema) | NOT PRODUCED — accepted | Same rationale as F1. The journey schema in `nw-design-methodology` is for multi-step user journeys; this feature has one step (operator runs one `stats` command with two new flags). |
| F3 | Journey artefact (Gherkin `.feature`) | INTENTIONALLY OMITTED | Per the project convention (inherited from `cli-read-time-range-v0` and noted in the task brief): this project's acceptance idiom is Rust `#[test]` functions with `// Given / // When / // Then` comment blocks, not Gherkin `.feature` files. The Given/When/Then text lives inside `user-stories.md` (UAT Scenarios section). DISTILL writes the Rust tests. |
| F4 | Shared artefact registry | NOT PRODUCED — accepted | The cross-feature shared artefacts (`tenant_id`, the operator-supplied data directory path, the plain-text `key=value\n` line shape, the ISO 8601 UTC text format, the half-open `[start, end)` interval contract, the named-flag fail-fast error format) are tracked in the reference features' registries and reused unchanged here. The new shared artefacts in this feature are: the D-CinderScope decision (the Cinder lines are state-snapshot, NOT time-bound) and the D-EmptyWindow contract (empty bounded window mirrors the predecessor's empty-tenant contract — exactly `records=0\n` with no `earliest=` / `latest=`). Both are pinned inline in the System Constraints in `user-stories.md` and exercised by acceptance tests. Producing a separate registry would copy material already pinned in source. |
| F5 | Story map | PASS | `discuss/story-map.md` with one-activity backbone, walking-skeleton justification (N/A — the substrate already exists), single-slice rationale, cross-feature alignment, scope assessment. |
| F6 | Prioritization | INTEGRATED into `story-map.md` | The single-slice structure renders a separate `prioritization.md` redundant. The `## Priority Rationale` section in `story-map.md` covers the ordering and partial-ship logic. |
| F7 | Outcome KPIs | PASS | `discuss/outcome-kpis.md` with OK1 (principal record count), OK2 (windowed earliest/latest), OK3 (Cinder invariance — pins D-CinderScope), OK4 (no-flag non-regression), each with quantitative target, baseline, and measurement plan. |
| F8 | User stories | PASS | `discuss/user-stories.md` with US-01, including complete Elevator Pitch with the real entry-point shell command (`kaleidoscope-cli stats acme /tmp/data --since 2026-05-18T00:00:00Z --until 2026-05-19T00:00:00Z`) and the concrete byte-level output (the three Lumen lines reflecting the half-open window plus the unchanged Cinder snapshot lines, with exact nanosecond literals computed in Domain Example 1). |
| F9 | Per-slice files | PASS | 1 file under `discuss/slices/`. |
| F10 | Wave decisions | PASS | `discuss/wave-decisions.md` with D-Optional (both-flags-optional), D-Interval (half-open semantics), D-CinderScope (Cinder is state-snapshot — the principal new design surface), D-EmptyWindow (empty-window contract), D-NoNewError (parser and error path reused), D-Locked-tests (mechanical signature-match update only), D-Test-file (new acceptance test file shape), D-OutOfScope (Cinder time-bound queries, `--observe-otlp` on `stats`, JSON output, other Lumen query parameters, legacy `stats()` modifications, time format variants, SSOT non-modification). |
| F11 | Right-sized (Elephant Carpaccio) | PASS | 1 story, 1 bounded context (`kaleidoscope-cli` crate), 2 modified files + 1 new test file + 1 manifest line-level change. Estimated effort: <= 1 day. STRICTLY THINNER than the predecessor `cli-read-time-range-v0` because the parser-side work is already done. `story-map.md` "Scope Assessment: PASS". |
| F12 | SSOT impact assessed | PASS | `wave-decisions.md` D-OutOfScope: no SSOT modification. Same posture as the five reference features. |
| F13 | Cross-feature contract honoured | PASS | `user-stories.md` System Constraints pin the half-open `[start, end)` semantics, the half-bounded defaults (`u64::MAX` upper, `0` lower), the ISO 8601 UTC accepted shape (`Z` suffix only, 0..=9 fractional-second digits) inherited from the existing parser, the no-flag default of `TimeRange::all()`, the order-independent flag parsing via the existing `parse_time_range(args)` helper, the no-flag non-regression invariant against the locked test files, the D-CinderScope decision (Cinder is state-snapshot), the D-EmptyWindow contract (empty window emits exactly `records=0\n`), and the D-NoNewError contract (same parse error path as `read`). The lumen `TimeRange` API contract from `crates/lumen/src/record.rs:97-120` is inherited unchanged. The cinder `TieringStore::list_by_tier` semantics are inherited unchanged. |

## Anti-pattern scan

| Anti-pattern | Detected? | Notes |
|--------------|-----------|-------|
| Implement-X | No | Story framed as "Priya runs `kaleidoscope-cli stats acme /tmp/data --since X --until Y` and sees records / earliest / latest reflecting only the half-open window, with the Cinder lines unchanged" — outcome-first from the operator perspective; the `TimeRange`-driving wiring is the means, not the end. |
| Generic data | No | Real names: Priya, `acme`, `globex`. Real paths: `/tmp/data`. Real ISO 8601 timestamps: `2026-05-18T00:00:00Z`, `2026-05-19T00:00:00Z`, `2026-05-19T15:30:00Z`, `2026-05-17T02:00:00Z`. Real nanosecond literals computed for those timestamps (e.g. `1_779_062_400_000_000_000` for the start of `2026-05-18T00:00:00Z`, `1_779_148_800_000_000_000` for `2026-05-19T00:00:00Z`). Real witness `observed_time_unix_nano` values in the scenarios (`{100, 200, 300, 400, 500}` chosen to make boundary inclusion/exclusion testable and demonstrable, with the windowed-earliest = 200 ns rendering as `1970-01-01T00:00:00.000000200Z`). Real source-file references with line numbers: `crates/kaleidoscope-cli/src/lib.rs:349-383`, `:359-361`, `:362-369`, `:375-380`, `:312-331`, `:528-647`; `crates/kaleidoscope-cli/src/main.rs:188-214`, `:197-214`, `:216-235`, `:226-235`; `crates/lumen/src/record.rs:97-120`, `:111-114`, `:116-119`. |
| Technical AC | No blocking instances | AC pin the observable invariants (which records are counted on the `records=` line, the byte-exact shape of the `earliest=` / `latest=` lines, byte-identity of the Cinder lines across different `TimeRange` invocations, byte equivalence under the no-flag default, fail-fast exit code with the offending flag named in stderr). The AC explicitly defers the exact signature shape on `stats_with_tiers` to DESIGN ("the exact signature shape is DESIGN's choice; the observable property is that the caller can drive any `TimeRange::new(s, e)` into the underlying `lumen.query` call"). |
| Technical scenario titles | No | Scenarios titled by user outcome: "Bounded window query reflects only records in `[since, until)`", "No flags is byte-equivalent to today's full-tenant summary", "Empty window emits `records=0` with no earliest/latest", "Cinder lines are state-snapshot, NOT time-bound", "Invalid ISO 8601 on `--since` fails fast with stderr message". Not "stats_with_tiers calls lumen.query with TimeRange::new" or "parse_iso8601_utc_nanos returns IsoParseError". |
| Oversized story | No | 1 story, 7 UAT scenarios (within the 3-7 envelope), 14 AC, <= 1 day effort. Right-sized. |
| Abstract requirements | No | Every AC has a numeric value (witness `observed_time_unix_nano` values, boundary inclusion/exclusion claims, byte-count claims on the Cinder lines, byte-equivalence claims), a string literal (flag names `--since` and `--until`, ISO 8601 shape `YYYY-MM-DDTHH:MM:SS[.NNNNNNNNN]Z`, the `Z` suffix, the line shapes `records=N`, `earliest=<ISO>`, `latest=<ISO>`, `hot=H`, `warm=W`, `cold=C`), or a byte-level invariant (stdout begins with `records=0\n` on empty window, Cinder lines byte-identical across two invocations, exit code 1 on parse failure). |

## Reviewer self-pass (Dimension 0 — Elevator Pitch, BLOCKING)

| Story | Section present | Real entry point | Concrete output | Job connection | Verdict |
|-------|-----------------|------------------|-----------------|----------------|---------|
| US-01 | yes (Before / After / Decision enabled triple) | yes — `kaleidoscope-cli stats acme /tmp/data --since 2026-05-18T00:00:00Z --until 2026-05-19T00:00:00Z` (the actual shell command an operator types on a real CLI subcommand) | yes — stdout contains the three Lumen lines (`records=N` where `N` is the count of records in `[1_779_062_400_000_000_000, 1_779_148_800_000_000_000)`, `earliest=<ISO 8601 UTC>` and `latest=<ISO 8601 UTC>` derived from the min/max `observed_time_unix_nano` WITHIN that window) followed by the unchanged Cinder snapshot lines (`hot=H` / `warm=W` / `cold=C` for tiers with non-zero per-tenant placements). The Elevator Pitch shows the exact nanosecond literals computed from the ISO 8601 boundary values and explicitly names the D-CinderScope contract (Cinder counts are time-independent). | yes — "Priya can decide 'how many records did `acme` write in yesterday's incident window?' — answered directly with one CLI invocation, in two text lines on stdout, without scrolling through every record. Also: 'Is yesterday's slice empty for tenant X?' — answered by the `records=0\n` shape. Also: 'What's the duration of the active window for this tenant?' — answered by `latest - earliest`." | PASS |

Slice-level check (Dimension 0 item 5): the single slice (Slice 01)
is tagged operator-visible (NOT `@infrastructure`). The story's
Elevator Pitch names a real user-invocable CLI command and the
byte-level output (the three Lumen lines plus the unchanged Cinder
snapshot) the operator sees on stdout. There is no slice-level
infrastructure-only blocking concern. PASS at slice level.

## Confirmation-bias detection (Dimensions 1-4 self-check)

| Dimension | Check | Verdict |
|-----------|-------|---------|
| 1.1 Technology bias | Story specifies the existing CLI surface (`kaleidoscope-cli stats`), the existing `lumen::TimeRange` data type, the existing ISO 8601 UTC format (already produced by the project's own `format_iso8601_utc_nanos` formatter), and the existing `parse_iso8601_utc_nanos` parser. No specific date-time library prescribed (parser is the existing hand-rolled one from `cli-read-time-range-v0`; explicit non-introduction of `chrono` and `time` inherited from D5 of the predecessor wave). No specific shell or terminal prescribed. | PASS |
| 1.2 Happy path bias | Story includes the no-flag byte-equivalence scenario (operator who omits the flags must see byte-equivalent behaviour), the half-bounded scenarios (one each for `--since`-only and `--until`-only), the empty-window scenario (D-EmptyWindow), the Cinder-invariance scenario (D-CinderScope), AND the invalid-input scenario (D-NoNewError). 7 scenarios total covering: 1 happy path with both flags, 1 happy path with no flags, 2 half-bounded happy paths, 1 empty-window path, 1 invariance-probe path, 1 sad path. Sad-shaped paths (empty window, invariance probe, invalid input) are 3/7 = ~43% of the scenario surface; not happy-path-biased. | PASS |
| 1.3 Availability bias | The chosen pattern (reusing the existing `parse_time_range(args)` / `parse_flag_iso(args, flag)` helpers and the existing `parse_iso8601_utc_nanos` parser) is explicitly justified by `wave-decisions.md` D-NoNewError (helpers and parser are subcommand-neutral and reused unchanged on the `stats` side). The alternative of duplicating the parser is not even considered because it would violate the no-new-error-code constraint and produce two copies of the same hand-rolled state machine. The alternative of filtering Cinder by `placed_at` is explicitly considered and rejected in D-CinderScope with detailed reasoning about Cinder's API surface (`TierEntry` has `placed_at` but no "currently in tier at time T" query) and operator semantics. | PASS |
| 2.1 Missing stakeholder perspectives | Primary: platform operator (Priya). Secondary: Lumen maintainer (covered by the no-modification-to-`lumen::TimeRange` claim — the storage layer's contract is honoured, not extended). Tertiary: Cinder maintainer (covered by the no-modification-to-Cinder claim — D-CinderScope explicitly leaves the Cinder code path untouched). Quaternary: existing CLI test maintainers (covered by the meta-AC — the existing locked test files continue to pass byte-equivalently with no assertion edits, only mechanical signature-match updates). | PASS |
| 2.2 Missing error scenarios | Three sad-shaped scenarios: invalid `--since` (D-NoNewError), empty window (D-EmptyWindow — a degenerate but valid query result), and the Cinder-invariance probe (which detects a future regression that would silently filter the Cinder lines). The CLI failure mode is exit code 1 with the offending flag named in stderr; the inline tests at `crates/kaleidoscope-cli/src/main.rs:510-553` already cover the helper-level invalid-input contract for both flag names. Out-of-range calendar components (e.g. month 13, day 32, hour 25) are covered by the predecessor `read_time_range.rs` tests #5 and #6 against the SAME helper. | PASS |
| 2.3 Missing NFRs | NFRs covered: stdout byte equivalence under the no-flag default (OK4), half-open boundary correctness (OK1, exercised by witness records at exactly the boundary values 200 and 400), windowed earliest/latest correctness (OK2), Cinder-invariance under different time ranges (OK3 / D-CinderScope), empty-window contract (D-EmptyWindow), fail-fast invariant (D-NoNewError — no Lumen or Cinder store opened on invalid input, no bytes written to stdout, same error path as `read`). Thread-safety and concurrency are N/A (single-threaded function, no new concurrency introduced — Cinder still uses its `NoopRecorder` here). | PASS |
| 3.1 Vague performance requirements | All "100%"/"0%" claims are quantitative and falsifiable via the named test. No "fast", "scalable", "performant" adjectives without a number. The "ten gigabytes of NDJSON" figure in the problem statement is a realistic-magnitude framing of the existing pain, not a performance target for the new feature. | PASS |
| 3.2 Ambiguous requirements | Every AC pins exact string literals (`--since`, `--until`, the ISO 8601 shape `YYYY-MM-DDTHH:MM:SS[.NNNNNNNNN]Z`, the `Z` suffix, the line shapes `records=N`, `earliest=<ISO>`, `latest=<ISO>`, `hot=H`, `warm=W`, `cold=C`), exact integer invariants (`since_ns`, `until_ns`, exit code 1, the witness `observed_time_unix_nano` values, the windowed-earliest and windowed-latest renderings as `1970-01-01T00:00:00.000000200Z` and `1970-01-01T00:00:00.000000300Z`), exact byte invariants (stdout begins with `records=0\n` on empty window; Cinder lines byte-identical across two `TimeRange` invocations), or exact set memberships (the record at exactly `until_ns` is EXCLUDED; the record at exactly `since_ns` is INCLUDED). | PASS |
| 4 Testability | Every AC is a property of stdout bytes (after `stats_with_tiers()` returns), the function's return value, the stderr text (for D-NoNewError), or the process exit code (for D-NoNewError). Every AC is automatable via byte comparison, line-prefix checking, set membership, `Result::is_err()` + substring search on the error message, or two-invocation byte-identity comparison. Test names are pre-sketched in the slice file. | PASS |
| 5.1 Largest bottleneck | The largest gap in the operator's per-incident triage workflow is "the `stats` subcommand can only summarise the full tenant, so getting yesterday's count and earliest/latest requires dumping records via `read --since X --until Y` and aggregating client-side via `jq`". This feature addresses exactly that gap. | PASS |
| 5.2 Simpler alternatives considered | Considered and rejected: (a) Filtering Cinder by `placed_at` within the window — rejected in D-CinderScope because Cinder exposes no "currently in tier at time T" query and the aggregation semantics have no obvious right answer. (b) Adding `--since` / `--until` to the legacy 3-arg `stats()` function as well — rejected because `stats()` is the byte-level OK4 oracle for the original `cli-stats-subcommand-v0` feature and stays untouched. (c) Emitting `earliest=null` / `latest=null` lines for the empty-window case — rejected in D-EmptyWindow because it would diverge from the predecessor's empty-tenant contract and worsen the signal-to-noise ratio. (d) Introducing a new flag like `--cinder-at <ISO>` for a time-bound Cinder query — documented as a future feature candidate, explicitly out of scope here. (e) Adding a `--json` output flag — out of scope, future candidate. | PASS |
| 5.3 Constraint prioritization | The dominant constraint is OK1 + OK2 (bounded-window record count plus windowed earliest/latest derivation, including the boundary inclusion/exclusion behaviour). OK3 (D-CinderScope guardrail), OK4 (no-flag non-regression), D-EmptyWindow (empty-window contract), and D-NoNewError (parser + error reuse) are correctly weighted as the necessary design-decision pin, the necessary non-regression guarantee, the necessary degenerate-case contract, and the necessary failure-mode-shape constraint. | PASS |
| 5.4 Data-justified | This is not a performance optimization (no profiling data needed). The decision to extend the existing `stats` subcommand with two new optional flags is justified by the operator's existing muscle memory (the `parse_time_range(args)` helper at `crates/kaleidoscope-cli/src/main.rs:188-214` makes the new flags' parsing posture obvious because the same helper is already used by `read`) and by the storage layer's existing `TimeRange::new(s, e)` entry point (no new storage-layer API surface needed). | PASS |

## DoR Status: PASSED — with four honestly-recorded artefact gaps

The story-level DoR (9 items × 1 story = 9 checks) is PASSED with
evidence on every item. The feature-level DoR is PASSED with the
following honestly-recorded artefact gaps:

- **F1, F2 (journey artefacts)**: deliberately not produced. The
  journey is one step (operator runs `stats` with two new optional
  flags, sees the three Lumen lines reflecting the half-open window
  plus the unchanged Cinder snapshot on stdout); a multi-step
  journey visual or YAML would duplicate the Elevator Pitch in
  US-01. The five reference features' journey visuals collectively
  cover the underlying mental model. Acknowledged; will not be
  remediated.
- **F3 (Gherkin `.feature` file)**: deliberately not produced. Per
  the project convention, this project's acceptance idiom is Rust
  `#[test]` functions with `// Given / // When / // Then` comment
  blocks. The Given/When/Then specification lives inline in
  `user-stories.md`.
- **F4 (shared artefact registry)**: not produced. The cross-feature
  shared artefacts are tracked in the reference features' registries
  and reused unchanged; the new shared artefacts (D-CinderScope
  decision and D-EmptyWindow contract) are pinned inline in
  `user-stories.md` System Constraints and exercised by acceptance
  tests. Producing a new registry that copies the reference
  features' rows would add no value.
- **F6 (separate `prioritization.md`)**: not produced; integrated
  into `story-map.md`'s `## Priority Rationale` section per the
  `nw-leanux-methodology` skill template, because a one-slice
  feature does not earn a separate prioritization document.

Anti-pattern scan clean. Dimension 0 elevator-pitch check PASSES
with the real entry point (`kaleidoscope-cli stats acme /tmp/data
--since 2026-05-18T00:00:00Z --until 2026-05-19T00:00:00Z`) and the
concrete byte-level output (the three Lumen lines plus the
unchanged Cinder snapshot lines, with exact nanosecond literals
computed in Domain Example 1 and the explicit D-CinderScope contract
named in the After clause). Dimensions 1-5 confirmation-bias
self-check PASSES.

The DISCUSS wave is ready to hand off to `nw-solution-architect`
for DESIGN.
