# Definition of Ready — Validation

Feature: `cli-migrate-subcommand-v0`
Reviewer: Luna (self-review at DISCUSS close; second pass scheduled
at handoff to DESIGN per `nw-po-review-dimensions` skill).
Date: 2026-05-19.

## Per-story DoR (9-item hard gate)

### US-01: Operator manually moves a single item between Cinder tiers without writing Rust

| # | DoR Item | Status | Evidence |
|---|----------|--------|----------|
| 1 | Problem statement clear, domain language | PASS | "What she canNOT do today is move a single item — e.g. `acme/batch-00042` — between tiers from the CLI. To 'pull a batch back to Warm' after an over-aggressive auto-tiering policy, or to 'force a specific batch to Cold for rebalancing', she has to write a Rust harness ... She also has to remember which `from` tier the item was in, because the typed `MigrateError::UnknownItem` does not carry the previous tier ... Result: tier rebalancing is a Rust task, lifecycle testing is a Rust task, and the operator's mental model 'I move item X from Hot to Cold' has no direct CLI surface." Uses tier, migrate, ItemId, TieringStore, Cinder, MigrateError, UnknownItem, batch, hot/warm/cold, `place()`, `evaluate_at`, policy, `get_entry`, lower-case — all domain terms grounded in the existing Cinder/CLI implementation. |
| 2 | User/persona identified | PASS | Priya the platform operator, multi-tenant Kaleidoscope deployment for a fintech, already uses `kaleidoscope-cli ingest`, `kaleidoscope-cli read`, `kaleidoscope-cli stats` daily (inherited from the six predecessor features). Uses standard Unix text tools (`grep`, `cut`, `awk`) on stdout output. Expects fail-fast behaviour (non-zero exit + descriptive stderr) on unknown item ids and invalid tier names. Does NOT expect the subcommand to touch the Lumen store. |
| 3 | 3+ domain examples with real data | PASS | Five examples: happy path with `acme/batch-00042` Hot→Warm and the literal one-line stdout shown; edge case (idempotent same-tier) with `acme/batch-00007` Cold→Cold showing the underlying API IS idempotent per `crates/cinder/src/store.rs:167-188` and the CLI faithfully reports `from=cold to=cold`; error case (unknown item) with `acme/batch-00099` showing the empty-stdout + non-empty-stderr + non-zero-exit + no-store-mutation contract; error case (invalid tier) with `HOT` showing the same contract on the parse side; tenant-isolation case with `acme` and `globex` both holding an item id `acme/batch-00042` showing only `acme`'s item is mutated. Real values throughout: `acme`, `globex`, `acme/batch-00042`, `acme/batch-00007`, `acme/batch-00099`, `acme/batch-00001`, `acme/batch-00000`, `/tmp/k-data`, `/tmp/kdata`, `/tmp/data`, real tier names (`hot`, `warm`, `cold`), real source-file references with line numbers. |
| 4 | UAT scenarios (3-7 in Given/When/Then) | PASS | 5 scenarios: happy-path Hot→Warm (OK1), idempotent same-tier Cold→Cold (OK4), unknown-item fail-fast (OK2), invalid-tier fail-fast (OK3), tenant-isolation (`stats(acme)` does not mutate `globex`'s same-named item). |
| 5 | AC derived from UAT | PASS | 10 AC bullets, each maps to an observable byte-level / state-level invariant from a scenario (one literal stdout line on happy-path success; idempotent same-tier stdout line shows `from=current to=current`; post-call `get_entry().tier == to_tier` for happy-path; post-call `get_entry().tier == pre-call tier` for both error paths; non-zero exit + stderr containing offending item id for unknown-item; non-zero exit + stderr containing invalid tier value for invalid-tier; tenant-isolation `get_entry(globex, ...).tier` unchanged; Lumen store byte-equivalent before/after every invocation; new acceptance test file added; locked test files continue to pass green unmodified; `print_usage` update optional; no new external dependency). |
| 6 | Right-sized | PASS | 1 story, 2 function-level additions in `lib.rs` (new `migrate(...)` library function + new `tier_from_lowercase(...)` parser helper), 1 dispatch wiring change in `main.rs` (new `Some("migrate")` arm + new `run_migrate(...)` helper + `print_usage` update), 1 new test file with 5 scenarios, 1 manifest entry. Estimated effort: well under 1 day. Comparable to the immediate predecessor (`cli-stats-cinder-tier-distribution-v0`); the structural surface area is the same. |
| 7 | Technical notes identify constraints | PASS | File paths, line numbers, manifest entry, dependency posture (no new external dependency — `cinder::TieringStore::migrate`, `get_entry`, `MigrateError::UnknownItem`, `FileBackedTieringStore::open`, `cinder::NoopRecorder`, `Tier` enum, `ItemId::new`, `cinder_base()` helper, `tier_lowercase()` helper, `parse_positional` helper, `aegis::TenantId`, `std::time::SystemTime::now()` all already wired). The explicit non-prescription of internal implementation details beyond the choice of one new free function shape (DESIGN-locked per `wave-decisions.md` D-FunctionShape) and the choice of error variant (DESIGN-locked per D-ErrorVariant). The hard rule "DO NOT modify any locked test file" is restated as D-LockedTests. |
| 8 | Dependencies tracked | PASS | All Cinder-side APIs exist (`crates/cinder/src/store.rs:43, :89, :93-99, :179-182`); `cinder::FileBackedTieringStore` is already used by `ingest()` (`crates/kaleidoscope-cli/src/lib.rs:179-180`); `cinder::NoopRecorder` already used by `ingest`'s no-flag arm (`crates/kaleidoscope-cli/src/lib.rs:170-174`); `cinder::Tier` and `cinder::ItemId` already imported (`crates/kaleidoscope-cli/src/lib.rs:58`); `cinder_base(data_dir)` helper at `crates/kaleidoscope-cli/src/lib.rs:122-124`; `tier_lowercase` helper at `crates/kaleidoscope-cli/src/lib.rs:389-395`; `parse_positional` helper at `crates/kaleidoscope-cli/src/main.rs:248-254`; `aegis::TenantId` already a dependency. No unresolved internal or external dependencies. |
| 9 | Outcome KPIs defined with measurable targets | PASS | Maps to OK1-CLI-migrate-success (principal: 100% of valid invocations produce the exact stdout report line AND post-call `get_entry().tier == to_tier`), OK2-CLI-migrate-unknown-item-fail-fast (100% of unplaced-item invocations produce non-zero exit + stderr containing the item id + no store mutation), OK3-CLI-migrate-invalid-tier-fail-fast (100% of non-lower-case-hot/warm/cold invocations produce non-zero exit + stderr containing the invalid value + no store mutation), OK4-CLI-migrate-idempotent-same-tier (100% of same-tier invocations succeed faithfully — no CLI special case; the underlying API is idempotent per `crates/cinder/src/store.rs:167-188`). All four targets are quantitative and falsifiable via the named test file. |

### US-01 DoR Status: PASSED

## Feature-level DoR

| # | Item | Status | Evidence |
|---|------|--------|----------|
| F1 | Journey artefact (visual) | NOT PRODUCED — accepted | Per `wave-decisions.md`, the journey is so thin (one operator action: invoke `migrate <tenant> <data_dir> <item_id> <to_tier>` and read stdout) that a separate journey visual would duplicate the Elevator Pitch in US-01. The six reference features in this cluster collectively cover the underlying CLI operator mental model. Acknowledged gap; will not be remediated. |
| F2 | Journey artefact (YAML schema) | NOT PRODUCED — accepted | Same rationale as F1. The journey schema in `nw-design-methodology` is for multi-step user journeys; this feature has one step. |
| F3 | Journey artefact (Gherkin `.feature`) | INTENTIONALLY OMITTED | Per the project convention (inherited from the six reference features and noted in the task brief): this project's acceptance idiom is Rust `#[test]` functions with `// Given / // When / // Then` comment blocks, not Gherkin `.feature` files. The Given/When/Then text lives inside `user-stories.md` (UAT Scenarios sections). DISTILL writes the Rust tests. |
| F4 | Shared artefact registry | NOT PRODUCED — accepted | The cross-feature shared artefacts (`tenant_id` positional argument, `data_dir` positional argument, `lumen_base(data_dir)` and `cinder_base(data_dir)` helpers, `TieringStore::migrate` / `get_entry` / `list_by_tier` APIs, `LumenToPulseRecorder` / `NoopRecorder` quiescent recorder patterns, the lower-case tier rendering helper `tier_lowercase`) are tracked in the six reference features and the source code itself. The output-shape contracts (one literal stdout line on success; non-zero exit + stderr line on failure) are restated as System Constraints in `user-stories.md`. |
| F5 | Story map | PASS | `discuss/story-map.md` with one-activity backbone, walking-skeleton justification (N/A — the predecessor cluster's substrate already exists and the Cinder `TieringStore::migrate` API already exists), single-slice rationale, cross-feature alignment, scope assessment, priority rationale section, all four KPI guardrails articulated. |
| F6 | Prioritization | INTEGRATED into `story-map.md` | The single-slice structure renders a separate `prioritization.md` redundant. The `## Priority Rationale` section in `story-map.md` covers the ordering logic and partial-ship reasoning. |
| F7 | Outcome KPIs | PASS | `discuss/outcome-kpis.md` with OK1 (principal — migrate-success correctness), OK2 (unknown-item fail-fast), OK3 (invalid-tier fail-fast), OK4 (idempotent same-tier — guardrail against introducing a special-case CLI guard that diverges from the underlying API), each with quantitative target, baseline, and measurement plan. |
| F8 | User stories | PASS | `discuss/user-stories.md` with US-01, including complete Elevator Pitch with the real entry-point shell command (`kaleidoscope-cli migrate acme /tmp/data acme/batch-00042 cold`) and the concrete byte-level output (one literal stdout line shown in Domain Example 1, the same literal shape for Examples 2-5 with varying values). |
| F9 | Per-slice files | PASS | 1 file under `discuss/slices/` (`slice-01-migrate-subcommand-moves-item.md`, within the 100-line spirit of the cap when considering the body content; some additional clarifying sections expand it but the core slice brief — Goal, IN scope, OUT scope, Rejected alternatives, Learning hypothesis, AC, Dependencies, Reference class, Effort, DoD — fits the structural cap). |
| F10 | Wave decisions | PASS | `discuss/wave-decisions.md` with D1-D4 (pre-wave decisions), D-LowerCase (the explicit task-brief decision), D-Timestamp (no `--at` flag), D-NoLumenTouch (Cinder-only), D-FunctionShape (DESIGN-locked signature), D-ErrorVariant (DESIGN-locked variant naming), D-StderrWording (DESIGN-locked exact wording), D-NewTestFile (new file location), D-LockedTests (do-not-modify-locked-files hard rule), D-OutOfScope-Bulk / D-OutOfScope-Dryrun / D-OutOfScope-Observe / D-OutOfScope-Json (all four task-brief out-of-scope items), D-Idempotent (no special case for same-tier — faithful to underlying API), D-NoSSOT. |
| F11 | Right-sized (Elephant Carpaccio) | PASS | 1 story, 1 bounded context (`kaleidoscope-cli` crate), 2 modified files in `src/`, 1 new test file, 1 manifest line-level change. Estimated effort: well under 1 day. `story-map.md` "Scope Assessment: PASS". |
| F12 | SSOT impact assessed | PASS | `wave-decisions.md` D-NoSSOT: no SSOT modification. Same posture as the six reference features. |
| F13 | Cross-feature contract honoured | PASS | `user-stories.md` System Constraints pin the output-shape contract (one literal stdout line on success; non-zero exit + stderr line on failure), the positional-argument convention (extends the existing two-positional shape with two new positional arguments `<item_id> <to_tier>` — no new flag), the stream contract (stdout for success, stderr for failure — inherited from the binary's existing main.rs error printer at `crates/kaleidoscope-cli/src/main.rs:68-71`), the lower-case tier convention (mirrors the rendering side at `crates/kaleidoscope-cli/src/lib.rs:389-395`), the no-Lumen-touch invariant (NEW — `wave-decisions.md` D-NoLumenTouch), the tenant-isolation invariant (inherited from `cinder::TieringStore`), and the faithfulness-to-underlying-API contract for the idempotent same-tier case (NEW — D-Idempotent). The locked test files for the six prior features continue to pass green UNMODIFIED. |

## Anti-pattern scan

| Anti-pattern | Detected? | Notes |
|--------------|-----------|-------|
| Implement-X | No | Story framed as "Priya runs `kaleidoscope-cli migrate acme /tmp/data acme/batch-00042 cold` and sees the one-line from→to report on stdout in milliseconds, exit 0, without writing Rust" — outcome-first from the operator perspective. The three operator decisions enabled (manual rebalance / compensating policy decision / lifecycle test) are named explicitly. |
| Generic data | No | Real names: Priya, `acme`, `globex`. Real item ids: `acme/batch-00042`, `acme/batch-00007`, `acme/batch-00099`, `acme/batch-00001`, `acme/batch-00000`. Real paths: `/tmp/k-data`, `/tmp/kdata`, `/tmp/data`. Real tier names: `hot`, `warm`, `cold` (and the explicit anti-examples `HOT`, `lukewarm`). Real source-file references: `crates/kaleidoscope-cli/src/lib.rs:58, :122-124, :170-174, :179-180, :243-244, :389-395`; `crates/cinder/src/store.rs:43, :55-58, :71-72, :89, :93-99, :119, :140, :167-188, :179-182`; `crates/cinder/src/tier.rs:28-32, :54-56`; `crates/kaleidoscope-cli/src/main.rs:50-64, :68-71, :198-224, :226-246, :248-254`. |
| Technical AC | No blocking instances | AC pin the wire-observable invariants (one literal stdout line on success with all four fields; empty stdout + non-empty stderr + non-zero exit on each failure path; post-call `get_entry().tier` matches expectation for success; pre-call vs post-call `get_entry().tier` unchanged for each failure path; Lumen store byte-equivalence before and after every invocation; tenant-isolation invariant). They reference the structural mirror of the predecessor's recorder construction and `run_stats` helper shape as implementation patterns but do NOT prescribe novel internal mechanisms; the structural-mirror references are facts about the precedent, not novel design constraints. |
| Technical scenario titles | No | Scenarios titled by user outcome: "Operator moves an item from Hot to Warm — sees the from/to transition on stdout", "Operator re-issues the current tier as the target — idempotent same-tier migrate succeeds", "Operator types an item id that was never placed — fail-fast with stderr naming the missing item", "Operator types an invalid tier value — fail-fast with stderr naming the invalid value", "Tenant isolation — migrating `acme/batch-00042` for `acme` does not touch `globex`'s same-named item". Not "TieringStore::migrate is called with arguments" or "FileBackedTieringStore::open recorder slot accepts boxed dyn trait". |
| Oversized story | No | 1 story, 5 UAT scenarios, 10 AC, well under 1 day effort. Comparable to the predecessors. |
| Abstract requirements | No | Every AC has a specific value (string literal `migrated tenant=`, `from=`, `to=`, the `\n` line terminator, substring assertions for stderr content), a numeric exit code (0 for success, non-zero for failure), or a state-level invariant (pre-call vs post-call `get_entry().tier` equality; Lumen directory byte-equivalence). |

## Reviewer self-pass (Dimension 0 — Elevator Pitch, BLOCKING)

| Story | Section present | Real entry point | Concrete output | Job connection | Verdict |
|-------|-----------------|------------------|-----------------|----------------|---------|
| US-01 | yes (Before / After / Decision enabled triple) | yes — `kaleidoscope-cli migrate acme /tmp/data acme/batch-00042 cold` (the actual shell command an operator types — NEW subcommand on the existing binary) | yes — one literal stdout line (`migrated tenant=acme item=acme/batch-00042 from=hot to=cold`) shown in the "After" line; companion error-case stderr line behaviour described concretely (`acme/batch-00099` named verbatim, `HOT` named verbatim) | yes — "Priya can decide and execute three operationally distinct moves from one CLI invocation: 1. 'Move this specific batch from Hot to Cold because we're rebalancing' ... 2. 'Auto-tiering policy moved this item too aggressively; pull it back to Warm' ... 3. 'Test lifecycle behaviour by manually walking an item through Hot → Warm → Cold' ..." | PASS |

Slice-level check (Dimension 0 item 5): the single slice (Slice 01)
is tagged operator-visible (NOT `@infrastructure`). The story's
Elevator Pitch names a real user-invocable CLI command and the
byte-level stdout output the operator's shell session will display.
There is no slice-level infrastructure-only blocking concern.
PASS at slice level.

## Confirmation-bias detection (Dimensions 1-4 self-check)

| Dimension | Check | Verdict |
|-----------|-------|---------|
| 1.1 Technology bias | Story specifies the existing CLI binary, the existing `TieringStore` trait API, and a plain-text one-line stdout shape. No specific JSON parser prescribed; no specific datetime library prescribed (`SystemTime::now()` from `std::time`); the new tier-argument parser is a four-line `match` against three string literals. The output is consumable by standard Unix text tools that any operator already has. | PASS |
| 1.2 Happy path bias | Story includes three non-happy-path scenarios out of five: unknown-item fail-fast (OK2), invalid-tier fail-fast (OK3), tenant-isolation guardrail. The idempotent same-tier scenario (OK4) is also a non-trivial edge case that exercises the underlying API's idempotence and protects against accidentally introducing a CLI special case that diverges from the trait contract. The OK2 and OK3 KPIs are themselves sad-path-shaped guarantees. | PASS |
| 1.3 Availability bias | The chosen output shape (plain-text key=value-on-one-line) is justified by the operator's existing tooling (Unix text utilities) and by the established conventions in the six predecessor features. The lower-case tier argument constraint (D-LowerCase) is justified by reference to the rendering-side convention at `crates/kaleidoscope-cli/src/lib.rs:389-395` and the OTLP-JSON serialisation convention. The `SystemTime::now()`-at-call-site decision (D-Timestamp) is justified against the alternative `--at` flag with explicit pointer to the existing `cinder::InMemoryTieringStore` API which accepts arbitrary `SystemTime` (so unit tests of the underlying trait can use a controlled clock without a CLI flag). | PASS |
| 2.1 Missing stakeholder perspectives | Primary: platform operator (Priya). Secondary: Cinder maintainer + Lumen maintainer (covered by the meta-AC — the existing locked test files for the six predecessor features continue to pass green UNMODIFIED, so the extension does not perturb any shipped feature). Tertiary: scripting/automation consumers (covered by the explicit operator-friendly stdout/stderr contracts: one literal line on success, one stderr line containing the offending value on failure, non-zero exit). | PASS |
| 2.2 Missing error scenarios | Two distinct error scenarios specified: unknown-item (OK2 — `MigrateError::UnknownItem`) and invalid-tier (OK3 — parse-side error before the migrate call). Plus the no-Lumen-touch invariant covering all three call branches (success, unknown-item, invalid-tier). Plus the tenant-isolation invariant. Plus the idempotent same-tier case which is technically a "no-op" success but is explicitly documented and tested. Cinder open errors are surfaced via the existing `kaleidoscope_cli::Error::CinderOpen(MigrateError)` variant (`crates/kaleidoscope-cli/src/lib.rs:77`) and bubble to the binary's existing error printer in `main()`. At most one new error variant introduced (the `InvalidTier { value: String }` variant for OK3, DESIGN's call). |
| 2.3 Missing NFRs | NFRs covered: migrate-success correctness (OK1 — operator-facing correctness invariant), fail-fast on unknown item (OK2 — operator-facing fail-fast guarantee), fail-fast on invalid tier (OK3 — operator-facing fail-fast guarantee), idempotence faithfulness (OK4 — guardrail against introducing a special-case CLI guard), no-Lumen-touch invariant (the Lumen WAL+snapshot is byte-equivalent before and after every call), tenant-isolation invariant (inherited from `cinder::TieringStore`). Performance: the v0 contract is "one `get_entry` call plus one `migrate` call per invocation" which is whatever the existing trait methods return; acceptable at v0 tier sizes. | PASS |
| 3.1 Vague performance requirements | All "100%"/"0%" claims are quantitative and falsifiable via the named test. No "fast", "scalable", "performant" adjectives without a number. The "in milliseconds" claim in the Elevator Pitch is descriptive operator language inherited from the six predecessor features, not a normative requirement; the AC do not quantify a latency target. | PASS |
| 3.2 Ambiguous requirements | Every AC pins exact string literals (`migrated tenant=`, `item=`, `from=`, `to=`, the `\n` line terminator), exit code values (0 for success, non-zero for failure), substring assertions on stderr (the verbatim item id for OK2; the verbatim invalid value for OK3), or state-level equality / inequality on `get_entry().tier`. | PASS |
| 4 Testability | Every AC is a property of (a) the captured stdout bytes after the migrate function returns, (b) the captured stderr bytes, (c) the returned `Result`, (d) a subsequent `cinder.get_entry(tenant, item)` call against a freshly-opened Cinder store, (e) a subsequent `list_by_tier(tenant, tier).len()` count, or (f) the presence/byte-content of the `lumen_base(data_dir)` directory. Every AC is automatable via `String::from_utf8` + `Vec<u8>` byte comparison + `str::contains` substring matching + `assert_eq!` integer / enum comparison + filesystem checksum or existence assertion. The OK4 idempotence is doubly testable: directly via the new test file's same-tier scenario AND indirectly via the absence of an "AlreadyInTier" branch in the library function (DESIGN MUST NOT introduce one). | PASS |
| 5.1 Largest bottleneck | The largest gap in the operator's daily Cinder workflow is "I want to move one specific item between tiers, but the only way today is to write a Rust harness against `cinder::FileBackedTieringStore::migrate` or run an off-cycle `evaluate_at` against a hand-tuned policy". This feature addresses exactly that gap by adding the missing CLI surface. | PASS |
| 5.2 Simpler alternatives considered | Considered and rejected: (a) bulk migration shape (`migrate <tenant> <data_dir> <item_ids_file>`) — rejected in `wave-decisions.md` D-OutOfScope-Bulk, single-item is the v0 primitive; (b) dry-run flag (`--dry-run`) — rejected in D-OutOfScope-Dryrun, operator's natural dry-run is `stats` before/after; (c) deterministic-time flag (`--at <timestamp>`) — rejected in D-Timestamp, `SystemTime::now()` is the only value a non-test caller needs and unit tests of the underlying trait use the `InMemoryTieringStore` directly; (d) mixed-case tier argument (`Hot`/`HOT`) — rejected in D-LowerCase, breaks symmetry with the rendering side and the OTLP-JSON serialisation; (e) a new atomic `TieringStore::migrate_observed(...) -> Result<TierEntry, MigrateError>` trait method — not introduced in v0, the read-then-write pattern is a known TOCTOU shape but the v0 use case (operator in a shell session) does not have a concurrent `evaluate_at` to race against. | PASS |
| 5.3 Constraint prioritization | The dominant constraint is correctness (OK1 — the report content must match the underlying API's outcome AND the post-call `get_entry().tier` must equal the requested `to_tier`). The two fail-fast guardrails (OK2 unknown-item, OK3 invalid-tier) are correctly weighted as the two distinct input-error paths the operator can encounter. OK4 (idempotent same-tier) is correctly weighted as the guardrail against accidentally introducing a CLI special case that diverges from the underlying API's documented behaviour. There is no cross-process or concurrency invariant to prioritize because the extension has only one read (`get_entry`) and one write (`migrate`) per invocation and runs in a single thread. | PASS |
| 5.4 Data-justified | This is not a performance optimization. The decision to use one `get_entry` call plus one `migrate` call per invocation (vs introducing an atomic `migrate_observed` trait method) is justified by the v0 scope (under-1-day effort, no new trait methods) and by the explicit learning hypothesis that the read-then-write pattern is sufficient at v0 use cases (operator in a shell session, no concurrent policy evaluation). The decision to hard-wire `SystemTime::now()` (vs introducing a `--at` flag) is justified by the existing in-memory adapter accepting arbitrary `SystemTime` for unit tests at the trait level. The decision to ship the single-item primitive (vs bulk migration) is justified by the standard "v0 ships the primitive, v1 ships ergonomic batching" pattern. | PASS |

## DoR Status: PASSED — with four honestly-recorded artefact gaps

The story-level DoR (9 items × 1 story = 9 checks) is PASSED with
evidence on every item. The feature-level DoR is PASSED with the
following honestly-recorded artefact gaps (inherited posture from
the six predecessor features):

- **F1, F2 (journey artefacts)**: deliberately not produced. The
  journey is one step (operator runs `migrate <tenant> <data_dir>
  <item_id> <to_tier>` and reads stdout); a multi-step journey
  visual or YAML would duplicate the Elevator Pitch in US-01.
  The six reference features' journey visuals collectively cover
  the underlying CLI operator mental model. Acknowledged; will
  not be remediated.
- **F3 (Gherkin `.feature` file)**: deliberately not produced.
  Per the project convention, this project's acceptance idiom is
  Rust `#[test]` functions with `// Given / // When / // Then`
  comment blocks. The Given/When/Then specification lives inline
  in `user-stories.md`.
- **F4 (shared artefact registry)**: not produced. The
  cross-feature shared artefacts are tracked in the six reference
  features and the source code itself; the output-shape contracts
  are restated as System Constraints in `user-stories.md`.
  Producing a new registry that copies the reference features'
  rows would add no value.
- **F6 (separate `prioritization.md`)**: not produced; integrated
  into `story-map.md`'s `## Priority Rationale` section per the
  `nw-leanux-methodology` skill template, because a one-slice
  feature does not earn a separate prioritization document.

Anti-pattern scan clean. Dimension 0 elevator-pitch check PASSES
with the real entry point (`kaleidoscope-cli migrate acme
/tmp/data acme/batch-00042 cold` — a NEW subcommand on the
existing binary) and the concrete byte-level output (one literal
stdout line on success; non-empty stderr line with the offending
value on each failure path). Dimensions 1-5 confirmation-bias
self-check PASSES.

The DISCUSS wave is ready to hand off to `nw-solution-architect`
for DESIGN.
